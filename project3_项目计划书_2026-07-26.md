# Project3 项目计划书

**多国纵向老龄化队列的死亡风险预测模型：开发、严格外部验证与跨文化可迁移性评价**

> **文档版本**：v2.0（2026-07-28 更新） | v1.0 原稿：2026-07-26
> **变更说明**：本版本基于 2026-07-26/27/28 的数据实测核查，对 7 项结构性决策进行了修正，并补入 12 项 v1 未涉及的新内容。全部变更有据可查，见 `docs/decision_log.md`。

---

## v2.0 相对 v1.0 的核心变更（读计划书前先看这7条）

| # | 项目 | v1.0 | v2.0 | 决策日志 |
|---|---|---|---|---|
| A1 | 队列数 | 7（含 JSTAR、ELSA） | **6**（CHARLS/CLHLS/KLoSA/SHARE/HRS/MHAS） | D-008、D-013 |
| A2 | 主结局 | **5 年**全因死亡 | **4 年**全因死亡 | D-010 |
| A3 | 主分析模型 | Cox 生存分析 | **pooled logistic / cloglog 离散时间** | D-010 |
| A4 | 次要时间窗 | 8 年 | **约 7 年**（CHARLS 波次结构） | D-010 |
| A5 | CHARLS harmonization | 跑 Gateway `.do` 文件 | **解析 `.do` 提取定义，用 Python/R 复刻** | D-007 |
| A6 | IC 定位 | 并行模块，可能进主模型 | **不进第一篇主模型**；Model C/D 降为敏感性分析 | D-012 |
| A7 | FI 项数 | 40+ 项（预估） | **41 项**（已实测冻结，剔除 hearaid/hlthlm_c/mbmicata） | D-011、D-020 |

**同时补入 v1 未涉及的关键内容**（B1–B12，见 `docs/plan_v2_changelog_pending_2026-07-27.md`）：
CHARLS ID 桥接规则（`householdID+"0"+末两位`）、2015/2018 波无死亡日期的实证依据、ADL 跳转逻辑（`.do` 6484 行）、BMI `qh006`/`qi002` 陷阱、IC 分母口径问题、时间精度不对称、KLoSA 变量命名每波不同、ELSA 死亡随访止于 2012 的实测证据等。

---


| 计划周期 | 2026-07-27 起 24 周（含 4 周缓冲） |
| 项目根目录 | `D:\AI_project\project3\` |
| 原始数据目录 | `D:\AI_project\sql\`（**只读，禁止任何写操作**） |
| 证据基础 | 82 份本地 PDF，26 篇进入核心证据矩阵（详见 `results\literature_extract\`） |
| 适用对象 | 首次接触老年公开数据库研究、无医学背景的研究者 |

---

## 第 0 章 如何使用本计划书

这份文档同时是三样东西，请按需要分层阅读：

1. **学习材料**（第 2 章、第 16 章）。你没有医学背景，第 2 章把这个领域的 8 个核心概念讲清楚。这一章不读完，后面的方法学决策会看不懂为什么要这么做。
2. **行动手册**（第 4.5 节、第 5-9 章、第 11 章）。里面的任务都写到了具体文件路径和具体判定标准，可以直接交给 Codex 执行，或者你自己一条条打勾。
3. **风险清单与答辩准备**（第 13 章、第 15 章）。第 15 章是我模拟的 SCI 审稿意见。在动手写代码之前先看一遍，能避免大约 80% 的返工。

阅读顺序建议：第 1 章 → 第 2 章（慢读）→ 第 4 章（这是本次盘点发现的真实问题，必须先做决策）→ 第 3 章 → 第 15 章 → 然后再进入第 5-9 章的执行细节。

**本文档中的三种标记：**

- 🔴 **决策点**：需要你本人做判断，我给出推荐但不能替你决定。
- 🟡 **待核验**：我尚未在数据中确认，必须先跑一遍核验脚本才能写进论文。
- ⚙️ **可交付 Codex**：纯执行性任务，规格已写清。

---

## 第 1 章 项目一页纸速览（v2.0）

**一句话版本。** 用中国的老龄化队列训练一个「**4 年死亡风险**」预测模型，然后拿到韩国、欧洲、美国、墨西哥的同类队列上原封不动地测试，看它是否还准，并解释为什么会变准或变差。

**一段话版本。** 老年健康研究里有很多预测模型，但绝大多数只在自己那一个数据库里验证过。当一个模型换到另一个国家、另一套问卷、另一种医疗制度下，它通常会变差——问题在于几乎没人系统地量化过「差多少」和「为什么差」。本项目以累积衰弱指数（Frailty Index, FI，41 项）为核心预测模块，以内在能力（IC）五域为并行模块（不进第一篇主模型），以 **4 年全因死亡**为主结局，在中国队列（CHARLS）开发模型，然后按「中国内部 → 亚洲内部 → 跨洲」三级阶梯做严格外部验证，并把每一次性能下降分解为事件率差异、协变量分布漂移、测量不等价三个来源。

**这项研究的价值不在 AUC 有多高，而在于它回答了一个方法学问题：老年健康表型的预测价值在多大程度上是跨文化稳定的。**

**三个 Aim（v2.0，队列数 7→6，ELSA/JSTAR 移出，MHAS 加入）：**

| Aim | 开发集 | 目标集 | 回答的问题 |
|---|---|---|---|
| **Aim 1** | CHARLS 2011（中国，45+） | CLHLS 2011/12（中国，65+，大量80+） | 同国家、不同年龄谱和调查框架，模型是否稳定 |
| **Aim 2** | {CHARLS}/{CHARLS+CLHLS}/{CHARLS+CLHLS+KLoSA} 轮转 | LOCO 留出队列 | 增加亚洲训练多样性能否改善迁移（含降采样对照） |
| **Aim 3** | CHARLS+CLHLS+KLoSA（亚洲池） | HRS（美国）/ SHARE（欧洲27国）/ MHAS（墨西哥）各自独立 + SHARE 内 leave-one-country-out | 跨大洲、跨收入水平的迁移极限 |

**已实测的事件数（60+ 子样本，2026-07-29 最终确认，含 D-021/D-022/D-023/D-024）：**

| 队列 | 基线 | 4 年窗口 | 60+ 事件 | 角色 |
|---|---|---|---|---|
| CHARLS | 2011 | →2015 | **785** | 开发 |
| CLHLS | 2011/12 | ≤1461天 | **3,502** | Aim 1 验证 |
| KLoSA | 2012 | →2016 | **585** | Aim 2 LOCO |
| SHARE | 2011 | →2015 | **3,689** | Aim 3 验证 |
| HRS | 2012 | →2016 | **2,352** | Aim 3 验证 |
| MHAS | 2012 | →2016 | **1,404** | Aim 3 验证 |

全部超过外部验证 ≥100 事件门槛。CHARLS EPV = 785/10 = **78**（远超 ≥20 要求）。

**核心产出：** 1 篇方法学导向的主论文 + 1 套可复现代码与变量协调表（Harmonization Table）+ 1 份 TRIPOD+AI 完整报告清单。

---

## 第 2 章 零基础知识铺垫

这一章不含任何项目决策，纯粹是为了让你能读懂后面的内容。如果某段读不懂，标记出来，这是正常的——这些概念在流行病学里通常要一学期。

### 2.1 什么是「老龄化纵向队列」

**队列（cohort）** 就是一群被长期追踪的人。研究者在某一年找到一批人（比如 45 岁以上的中国居民），问一大堆问题、做一些体格测量，这叫**基线（baseline）**。然后每 2-3 年再回访一次，这叫**波次（wave）**。谁死了、谁失能了、谁搬走联系不上了，都记录下来。

本项目用到的 6 个队列都属于同一个「家族」——它们都是模仿美国 HRS（Health and Retirement Study）设计的，所以问卷结构高度相似。这是本研究可行的技术前提，也是学界称之为 **HRS sister studies** 的原因。

| 队列 | 全称 | 国家/地区 | 起始年 | 目标人群 |
|---|---|---|---|---|
| HRS | Health and Retirement Study | 美国 | 1992 | 50+ |
| CHARLS | China Health and Retirement Longitudinal Study | 中国 | 2011 | 45+ |
| CLHLS | Chinese Longitudinal Healthy Longevity Survey | 中国 | 1998 | 65+（偏高龄，含大量 80+/百岁老人） |
| KLoSA | Korean Longitudinal Study of Ageing | 韩国 | 2006 | 45+ |
| SHARE | Survey of Health, Ageing and Retirement in Europe | 欧洲 27 国 | 2004 | 50+ |
| ELSA | English Longitudinal Study of Ageing | 英格兰 | 2002 | 50+ |
| MHAS | Mexican Health and Aging Study | 墨西哥 | 2001 | 50+ |

**你需要立刻记住的一个差异：** CLHLS 不是 CHARLS 的复制品。CLHLS 故意超额抽样了 80 岁以上和百岁老人，所以它的平均年龄远高于 CHARLS，死亡率也高得多。这个差异后面会成为 Aim 1 的核心科学看点——它不是数据缺陷，而是一次天然的「年龄结构压力测试」。

### 2.2 什么是「衰弱指数」（Frailty Index, FI）

这是本项目的主预测变量，务必弄懂。

**直觉：** 两个 75 岁的人，一个能爬山，一个连穿袜子都要人帮。年龄相同，但「生理年龄」差很远。衰弱指数就是把这种差异量化成一个 0 到 1 的分数。

**做法（Rockwood 缺陷累积模型）：** 列一张健康问题清单（叫 **deficits，缺陷**），比如「有没有高血压」「能不能自己洗澡」「视力好不好」……然后：

```
FI = 这个人有问题的项目数 / 这个人被问到的项目总数
```

举例：清单有 40 项，某人在 12 项上有问题，FI = 12/40 = 0.30。

**关键规则（Searle 等 2008 年的经典标准，本项目严格遵守）：**

1. 每项缺陷必须与健康状态相关，且随年龄增加而更常见。
2. 每项缺陷的患病率不能太低（一般 > 1%），否则没有区分力。
3. 缺陷之间不能高度重复（比如不要同时放「能否走 100 米」和「能否走 1 公里」两个近乎同义的项目）。
4. 项目要覆盖多个生理系统（心血管、认知、感觉、功能、心理等），不能全是心血管。
5. **每项都要编码到 0-1 区间**：二分类的直接 0/1；多分类的按等级映射，如「完全不能/很困难/有点困难/没问题」→ 1/0.67/0.33/0。
6. **缺失处理：** 分母只算「有回答」的项目。但如果一个人缺失项目过多（本项目规定 > 20%），整体 FI 不可信，应删除该个体。

**为什么本项目用 FI 而不用 Fried 表型？** Fried 表型需要握力和步速的实测值，很多队列的很多波次没做，跨队列覆盖率会崩掉。FI 只需要问卷条目，是 6 个队列都能构建的少数指标之一。这是**可行性驱动的方法学选择**，论文里要明确写出来。

**FI 的常用切点（仅用于描述，不用于建模）：** < 0.10 稳健（robust）；0.10-0.21 衰弱前期（pre-frail）；> 0.21 衰弱（frail）。本项目建模时用**连续 FI**，因为切二分类会丢信息。

### 2.3 什么是「内在能力」（Intrinsic Capacity, IC）

**来源：** WHO 在 2015 年《世界老龄化与健康报告》提出的概念，2019 年通过 ICOPE 指南推广。

**直觉：** FI 数「你有多少问题」，IC 数「你还剩多少能力」。一个是负向清单，一个是正向储备。WHO 主张后者更适合做健康老龄化的政策指标。

**五个域（ICOPE 官方定义）：**

| 域 | 英文 | 通常怎么测 |
|---|---|---|
| 运动 | Locomotion | 步速、起坐测试、行走困难 |
| 认知 | Cognition | 定向、词语回忆、连续减 7 |
| 活力 | Vitality | 体重下降、BMI、握力、疲乏 |
| 感觉 | Sensory | 自评视力、自评听力 |
| 心理 | Psychology | 抑郁量表（CES-D / EURO-D）、生活满意度 |

**IC 的最大陷阱（这条决定了本项目 IC 模块的定位）：** IC 各域的测量工具在不同国家差异极大。抑郁在中国用 CES-D-10，在欧洲 SHARE 用 EURO-D，题目数量、内容、计分方式都不同。认知测试的词语回忆表，中英文难度不一样。如果直接把「CHARLS 的 IC 分」和「SHARE 的 IC 分」当成同一个东西放进模型，就是在比较两把刻度不同的尺子。

**这就是为什么本项目把 IC 定为「并行模块」而不是主预测因子。** 只有通过测量等价性检验（见 6.5 节）后，IC 才能进主模型；否则只能作为增量预测和敏感性分析。这个谨慎态度本身就是论文的方法学贡献之一。

### 2.4 什么是「预测研究」，它和「因果研究」有什么不同

这是全项目最容易犯的概念错误，也是审稿人最爱抓的点。

| | 因果 / 病因学研究 | 预测研究（本项目） |
|---|---|---|
| 问题 | 「衰弱**导致**死亡吗？」 | 「用衰弱能**算出**谁会死吗？」 |
| 关心 | 效应的无偏估计 | 预测的准确度 |
| 协变量作用 | 混杂控制（要考虑因果图） | 提高预测（只要有用就能放） |
| 典型输出 | HR、OR、置信区间 | AUC、C-index、校准曲线 |
| 能否说「因为」 | 在严格假设下可以 | **不能** |
| 主要偏倚担忧 | 混杂、选择偏倚 | 过拟合、数据泄漏、乐观偏差 |

**具体后果举例：** 在预测模型里，「正在吃降压药」是个好预测因子（说明这人有高血压且在就医）。但在因果研究里，它可能是中介或碰撞变量，乱放会带来偏倚。本项目是预测研究，所以可以放；但**绝不能**因此说「吃降压药导致死亡风险升高」。

**写作红线：** 全文禁用「导致」「影响」「作用」「效应」这类因果动词描述模型结果。改用「与……相关联」「对预测有贡献」「在模型中权重较高」。

### 2.5 什么是「外部验证」，为什么它是本项目的核心

**内部验证（不够）：** 把一个数据库随机切成 70% 训练 + 30% 测试。问题是这两半来自同一批人、同一份问卷、同一个抽样框，性能会被高估。这只能证明「模型没写错代码」。

**外部验证（本项目要求）：** 在一个**完全独立**的队列上测试。三个层级，严格程度递增：

1. **时间外部验证** —— 同队列不同时期。最弱。
2. **地理/机构外部验证** —— 不同人群，相似测量工具。中等。本项目 Aim 1（CHARLS → CLHLS）属于这一级偏强的版本。
3. **跨文化/跨制度外部验证** —— 不同国家、不同语言、不同医疗体系、不同问卷。最强。本项目 Aim 2、Aim 3 属于这一级。

**「冻结」（freeze / lock）的含义** —— 这个词在本计划书出现几十次，务必理解：

在碰目标队列之前，以下**全部**要固定下来并写入文档，之后不许改：
- 纳入哪些变量（变量清单）
- 每个变量怎么编码（编码规则）
- 缺失怎么填（插补模型的参数）
- 连续变量怎么标准化（用开发集的均值和标准差，不是目标集的）
- 用哪个算法、什么超参数
- 风险分层的切点

**为什么这么严格？** 因为只要你在看过目标队列表现后调整了任何一项，「外部验证」就退化成「在目标队列上做了内部验证」，结论作废。审稿人会通过检查方法学时间线来查这一点。

**操作建议：** 冻结时把模型对象序列化保存（Python 用 joblib / pickle，R 用 saveRDS），连同预处理管线一起打包，加上 SHA256 校验和与时间戳，写进版本控制。这样任何人都能验证你没有偷偷改过。

### 2.6 判别、校准、临床效用：评价模型的三把尺子

绝大多数医学 AI 论文只报第一把尺子，这是本项目要超越的地方。

**（1）判别（Discrimination）—— 能不能把死的和活的排开**

- **AUC / C-statistic**：随机取一个死者和一个生者，模型给死者更高风险的概率。0.5 = 瞎猜，1.0 = 完美。老年死亡预测的现实区间是 0.75–0.85（主要由年龄贡献）。
- **Harrell C-index**：AUC 在生存数据（有删失）下的推广。
- **Uno C-index**：对删失做了 IPCW 加权修正的版本，跨队列比较时更稳健，建议两个都报。
- **Time-dependent AUC**：在具体某个时间点（如第 5 年）的 AUC。因为「5 年内死亡」的判别力和「10 年内死亡」不同。

**关键认知：AUC 高不等于模型有用。** 一个只用年龄的模型在老年队列里 AUC 就能到 0.72。所以必须报告「加入 FI 后 AUC 提升了多少」（ΔAUC），并做 DeLong 检验或 bootstrap 比较。

**（2）校准（Calibration）—— 预测的概率数值准不准**

模型说这 100 个人「5 年死亡风险 20%」，实际死了多少个？如果死了 20 个，校准完美；死了 40 个，模型严重低估。

- **Calibration-in-the-large（校准截距）**：整体预测风险 vs 实际风险。理想值 0。这是跨队列迁移最容易崩的指标——因为不同国家死亡率本身不同。
- **Calibration slope（校准斜率）**：理想值 1。<1 表示预测过于极端（过拟合的典型表现），>1 表示预测过于保守。
- **校准曲线**：横轴预测风险、纵轴实际风险的平滑曲线，理想是 45 度对角线。建议用 loess 平滑，不要只画十分位点。
- **Brier score / IBS**：预测概率与实际结局的均方误差，越小越好。同时包含判别和校准信息。

**为什么校准对本项目至关重要？** 跨队列迁移时，判别力常常还行（0.78 → 0.74），但校准可能完全崩掉（中国队列训练的模型用到 HRS 上系统性低估或高估）。**这个「判别保留、校准崩溃」的模式如果被观察到，本身就是一个高价值的发现**，因为它说明衰老表型的「排序信息」可跨文化迁移，而「绝对风险水平」不可迁移。这比「模型表现好」更有意思。

**（3）临床效用（Clinical utility）—— 拿这个模型做决定，比不做决定好吗**

- **决策曲线分析（DCA）**：横轴是「阈值概率」（你认为多高的风险才值得干预），纵轴是「净获益」。同时画三条参考线：本模型、「全部干预」、「全部不干预」。只有本模型的曲线在合理阈值范围内高于两条参考线，才能说模型有实际价值。
- **本项目阈值范围建议预先设定为 5%–30%**，对应老年综合评估、社区随访等低成本干预场景，并在方案中写明理由（避免事后挑选有利阈值）。

### 2.7 术语速查表

| 术语 | 一句话解释 | 本项目在哪用 |
|---|---|---|
| Wave（波次） | 队列的一次调查 | CHARLS 有 2011/13/15/18/20 五波 |
| Baseline（基线） | 用来做预测的时间点 | 主分析用各队列指定的基线波 |
| Censoring（删失） | 随访结束时还活着，或中途失联 | 生存分析必须处理 |
| Right-censored | 只知道「到某时点还活着」 | 本项目的标准情况 |
| Interval-censored | 只知道死在两波之间 | 队列数据的常见现实，见 5.4 |
| Person-years（人年） | 随访时间总和，死亡率的分母 | 描述表要报 |
| Attrition（失访） | 参与者退出但没死 | 需要和死亡区分开，见 5.4 |
| EPV / EPP | 每个变量对应几个死亡事件 | 样本量论证，见 8.1 |
| Imputation（插补） | 填补缺失值 | 用 MICE，见 8.2 |
| MICE | 链式方程多重插补 | 主分析方法 |
| Data leakage（数据泄漏） | 训练时用到了本不该知道的信息 | 最严重的技术错误 |
| Overfitting（过拟合） | 模型记住了噪声 | 用交叉验证 + 正则化控制 |
| SHAP | 把预测拆成各变量贡献的方法 | 解释模块，不是因果 |
| SMD | 标准化均差，比较两组分布差异 | 漂移诊断，>0.1 视为不可忽略 |
| PSI | 群体稳定性指数，衡量分布漂移 | >0.25 视为显著漂移 |
| Recalibration（重校准） | 在目标队列上调整截距/斜率 | 二次分析，必须与冻结模型并列报告 |
| TRIPOD+AI | 预测模型报告规范（2024 版） | 全文遵循 |
| PROBAST-AI | 预测模型偏倚风险评估工具 | 自评并附表 |
| LOCO | Leave-One-Cohort-Out，留一队列法 | Aim 2 核心设计 |
| Harmonization（协调） | 把不同数据库变量对齐成可比形式 | 本项目最耗时的一步 |
| DIF | 差异项目功能，题目在不同群体表现不同 | 测量等价性检验 |
| Frozen model（冻结模型） | 锁定后不许改的模型 | 外部验证的前提 |

---

# 第 3 章 研究问题、假设与创新点

## 3.1 主研究问题（Primary Research Question）

> **基于多国纵向老龄化队列的基线衰弱累积指数（FI，41 deficits）与共同协变量所构建的 4 年全因死亡预测模型，其判别力、校准度与临床效用能否在中国内部（CHARLS→CLHLS）、亚洲内部（→KLoSA）以及欧洲、美国、墨西哥队列（→SHARE / HRS / MHAS）中保持稳定；若不能，性能损失可分解为多少来自事件率差异、多少来自协变量分布漂移、多少来自测量不等价？**

请注意这个问法的三个刻意设计：

1. **它不问「模型准不准」，而问「在哪些条件下不准、为什么不准」。** 前者是工程问题，后者是科学问题。审稿人对后者的兴趣远高于前者。
2. **它预设了性能会下降。** 这是诚实的，也是文献支持的（C02: 0.934→0.792；C19: 0.838→0.695）。把「下降」当作研究对象而不是失败，是本项目的立论基础。
3. **它承诺了归因分解。** 这是本项目相对已有文献的最大增量。C03、C18、C19 都报告了外部 AUC 下降，但没有一篇系统地把下降拆解到「事件率 / 分布 / 测量」三个来源。

## 3.2 三个子目标（Aims）

### Aim 1：中国内部可迁移性（Internal-to-China transportability）

- **设计**：CHARLS 开发 → CLHLS 外部验证。
- **科学问题**：在同一国家、同一语言、但抽样框架（全国代表性中老年 vs 长寿地区高龄）、年龄结构（45+ vs 65+，CLHLS 大量 80+/百岁）、问卷措辞均不同的两个队列间，模型是否稳定？
- **为什么这是有意义的第一步**：它把「文化差异」这个混杂因素控制住了。如果 CHARLS→CLHLS 就已经崩了，那后面所有跨国结果都无法归因于「跨文化」，只能归因于「队列设计差异」。**这是本项目逻辑链的必要前提，不是可选步骤。**
- **预期**：C-index 由约 0.80 降至 0.72–0.76；校准截距明显偏移（CLHLS 高龄人群死亡率远高）。年龄结构差异会是主要驱动因素。

### Aim 2：亚洲多样性是否改善迁移（Does Asian diversity help?）

- **设计**：Leave-One-Cohort-Out (LOCO)，在 CHARLS / CLHLS / KLoSA 三个亚洲队列间轮转。每轮用两个队列开发、第三个队列测试，共 3 轮。
- **对照设计（关键）**：对每个目标队列，同时评价两个模型——(a) 仅 CHARLS 开发的单队列模型；(b) 加入另一个亚洲队列后的多队列模型。比较两者在目标队列上的性能差。
- **科学问题**：增加训练数据的**文化与制度多样性**（而不仅是样本量）能否提升迁移能力？
- **为什么这个对照必不可少**：如果不设置 (a)，你只能说「亚洲合并模型在亚洲队列内表现如何」，无法回答「多样性有没有用」。这是原方案中最需要修正的逻辑漏洞。
- **样本量混杂的处理**：多队列模型天然样本更大，性能提升可能来自样本量而非多样性。必须做**降采样对照**：把多队列训练集降采样至与单队列训练集相同的样本量和事件数，重复 200 次，报告性能分布。这是回应审稿人的关键防线。

### Aim 3：亚洲 → 欧美/拉美的跨文化迁移

- **设计**：亚洲池（CHARLS + CLHLS + KLoSA）开发 → SHARE、HRS、MHAS 分别独立验证（共3个目标队列）。
- **关于队列数**：原方案含 ELSA，但 ELSA Gateway harmonized G.3 的 `iwstat==5`（本波死亡）在 wave 7-9 全部为 0——EOL 文件仅覆盖 2002-2012，以 2012 为基线的 4 年窗口事件数为 0，无法构建结局（D-013）。ELSA 已移出。现有 3 个目标队列覆盖欧洲 27 国（SHARE）、美国（HRS）和墨西哥（MHAS）。其中 **MHAS（墨西哥）是中高收入国家**，与中国的可比性反而高于美国，这让论证从「亚洲 vs 西方」升级为「跨收入水平、跨医疗制度」。
- **附加分析（SHARE 内部）**：SHARE 覆盖 27 国，可做**国家级 leave-one-country-out**，得到 20+ 个独立目标国家的性能分布。这是本项目最有力的推广性证据，也是任何单队列研究做不到的。
- **结论边界**：即使全部完成，也只能表述为「在 3 个目标队列（HRS/SHARE/MHAS）+ SHARE 内 20+ 国的外部验证」。**不得声称「全球可推广」**，因为撒哈拉以南非洲、南亚、低收入国家完全未覆盖。这条边界必须写进 Discussion 的 Limitations，也必须写进 Abstract 的结论句。

## 3.3 预先注册的假设（Pre-specified hypotheses）

写下来并冻结，是为了防止事后把观察到的结果包装成「原本的假设」（HARKing，即 Hypothesizing After Results are Known）。

| 编号 | 假设 | 检验方式 | 若被否证意味着什么 |
|---|---|---|---|
| H1 | FI 在基础模型（年龄+性别+教育）之上带来显著判别增量，ΔC-index ≥ 0.02 | 嵌套模型比较 + bootstrap CI | FI 的预测价值可能被年龄吸收，需重新审视 FI 的独立性 |
| H2 | 判别力的跨队列衰减小于校准的衰减（即 ΔC 的相对损失 < 校准截距偏移的相对损失） | 各 source-target 对的标准化损失比较 | 若校准反而更稳，说明衰老表型的绝对风险可迁移，是更强的结论 |
| H3 | 加入第二个亚洲队列可改善对第三个亚洲队列的迁移，且在降采样后仍成立 | Aim 2 的 (a) vs (b) 对照 + 降采样 | 多样性无用 → 迁移瓶颈在测量而非人群构成，指向 Aim 3 的测量分析 |
| H4 | 性能损失中，事件率差异 + 协变量分布漂移可解释 ≥50%，剩余归于测量不等价与真实关系差异 | 逐步重校准阶梯（8.5 节） | 若残差占比很大，说明存在真实的效应异质性，是重要的机制发现 |
| H5 | FI 的迁移稳定性优于 IC 五域 | Model B vs Model C 的跨队列性能一致性 | IC 更稳 → 推翻「简单计数指标更稳健」的直觉，需在 Discussion 深入讨论 |
| H6 | SHAP 特征重要性排序在各目标队列间的 Spearman 相关 ≥0.7，年龄与 FI 始终位居前三 | 跨队列 SHAP 排序一致性 | 排序不稳 → 模型依赖队列特异的关联，泛化性论证需大幅收敛 |

## 3.4 创新点陈述（可直接改写为投稿信素材）

审稿人问「so what」时，你需要这四句话。它们按强度递减排序：

1. **首个把跨文化预测性能损失做机制归因分解的老龄化研究。** 已有的 CHARLS→ELSA/HRS 类研究（C18、C19）只报告 AUC 下降的事实。本项目用「事件率 → 分布 → 测量 → 残差」四层分解，把「下降」变成可解释的量。
2. **首个用降采样对照区分「数据多样性效应」与「样本量效应」的迁移研究。** 这个方法学细节看似技术，实际是 Aim 2 能否成立的唯一保证。
3. **7 队列、跨 4 大洲、含 1 个中高收入国家的验证广度，且全部使用严格的整队列留出。** 已有多队列 ML 研究通常 2–3 个队列（C03、C18），或虽多队列但做的是关联分析而非外部验证（C05、C06、C24）。
4. **同时评价 FI 与 ICOPE 五域 IC 两套表型的可迁移性，并预先通过测量等价性检验决定 IC 的角色。** 这回应了 C12 提出的「跨国 pooled 分析前应做潜在结构检验」的方法学呼吁，目前尚无预测模型研究照做。

## 3.5 本项目明确不做什么

写清楚边界，能省掉大量返工，也能在审稿时提前拆掉一半的攻击点。

- **不做因果推断。** 不声称 FI 或 IC「导致」死亡。全文用 predict / associated with，禁用 cause / effect / impact / reduce risk。
- **不做临床诊断工具。** 不部署、不提供 API、不做「医生决策支持」宣传。定位是人群健康与方法学研究。
- **不做算法竞赛。** 不为了多一个模型而加模型。模型库固定 5 个（8.3 节），理由预先写明。
- **不追求最高 AUC。** 若某个复杂模型 AUC 高但校准差或跨队列不稳，方案预先规定选择更稳健者，并报告全部结果。
- **不做生物标志物模块。** CHARLS 有血检但仅部分波次、部分子样本，KLoSA/CLHLS 覆盖不可比。纳入会使多队列共同变量集急剧缩小（C21 的教训）。留作单队列补充分析。
- **不合并不同量表直接算总分。** 未通过等价性检验的构念只作为并行模块或敏感性分析。

---

# 第 4 章 数据资源实际盘点（我已核对过磁盘）

这一章是本计划书中**最重要的现实约束**。我实际列出了 `D:\AI_project\sql\` 的目录内容，结果与你现有方案的假设有两处冲突。请先读 4.2 和 4.3。

## 4.1 磁盘现状总览

| 队列 | 目录 | 体积 | 数据形态 | 是否有 harmonized 版本 |
|---|---|---|---|---|
| CHARLS（中国） | `Charls/` | 831 MB | 原始分模块 `.dta`，5 个波次（2011/2013/2015/2018/2020） | ❌ **只有文档 PDF，无 H_CHARLS_D.dta** |
| CLHLS（中国） | `CLHLS/` | 498 MB | 原始 SPSS `.sav`，纵向文件（1998–2005、2008–2018）+ 2018 横断面 | ❌ 无（CLHLS 本身无官方 harmonized 版） |
| KLoSA（韩国） | `KLOSA/` | 1.2 GB | 原始 `.dta`，9 个波次 + w03–w08 EXIT 文件 | ❌ **无 H_KLOSA，需自行构建** |
| SHARE（欧洲 27 国） | `SHARE/` + `share harmonised/` | 6.2 GB + 2.0 GB | 原始 w1–w9 + **easySHARE rel9** + **Gateway `GH_SHARE_g.dta`** + EOL 模块 | ✅ **两套都有** |
| HRS（美国） | `HRS Products/` | 2.8 GB | RAND Fat Files 1992–2020 + **`harmonised HRS/H_HRS_d.dta`** | ✅ 有 |
| ELSA（英国） | `ELSA/` | 3.9 GB | UKDA 原始 + **`h_elsa_g3.dta`** + **`h_elsa_eol_a2.dta`** | ✅ 有 |
| MHAS（墨西哥） | `MHAS/` | 179 MB | **`H_MHAS_c2.dta`** + **`H_MHAS_EOL_b.dta`** | ✅ 有 |
| CHNS（中国） | `CHNS/` | 1.1 GB | 原始 + 已清洗 | — |
| **JSTAR（日本）** | — | — | **不存在** | ❌ **目录中没有** |

## 4.2 冲突一：JSTAR 不在磁盘上 —— 这直接影响 Aim 2 和 Aim 3

你现有方案（`AGENTS.md` 与 codex 报告的 Aim 2/Aim 3）把 JSTAR 作为亚洲第二/第三个队列，并设计了「CHARLS+CLHLS+KLoSA 开发、JSTAR 测试」与反向轮转的双向 LOCO。**但 `D:\AI_project\sql\` 中没有任何 JSTAR 文件。**

JSTAR 的现实情况：由日本 RIETI 主持，需向 RIETI 单独申请，审批周期以月计，且早期波次仅覆盖少数市町村（约 5–10 个自治体，样本量远小于其他队列，各波约 4,000–7,000 人）。它不是「下载即用」的资源。

**我的建议：放弃 JSTAR，改用 MHAS 扩展 Aim 3。** 理由：

1. **可行性**：MHAS 的 harmonized 文件已在磁盘上，且含 EOL（End-of-Life）模块，死亡信息完整。JSTAR 从申请到拿到数据的时间不可控。
2. **科学性反而更强**：JSTAR 只多加一个「东亚高收入国家」，与 KLoSA 高度相似，边际信息量低。而 MHAS 提供**中高收入国家**语境，ELSA 提供**与 HRS 制度不同的欧洲单国**语境。跨收入水平的验证比多一个东亚国家更有说服力。
3. **Aim 2 依然成立**：亚洲三队列（CHARLS、CLHLS、KLoSA）足以做 3 轮 LOCO。虽然 CHARLS 与 CLHLS 同属中国，但抽样框架差异极大（见 5.1），学术上可接受，前提是在 Limitations 中说明「亚洲多样性主要由中韩差异贡献」。

**如果你坚持要 JSTAR**：现在立刻启动 RIETI 申请（可与 Phase 1 并行），并在方案中把 JSTAR 标为 conditional cohort——即「若在 Phase 3 开始前到手则纳入，否则按预先规定的方案排除」。这个条件语句必须写在分析计划里，不能事后决定。

## 4.3 冲突二：CHARLS 与 KLoSA 没有 harmonized 版本 —— 这是最大的工作量

这一点决定了你 60% 以上的工程时间去向。

- **SHARE / HRS / ELSA / MHAS** 都有 Gateway to Global Aging 的 harmonized 文件（`GH_SHARE_g.dta`、`H_HRS_d.dta`、`h_elsa_g3.dta`、`H_MHAS_c2.dta`）。这些文件的变量命名规则统一（如 `r{wave}adla`、`r{wave}shlt`、`radyear`），跨队列变量含义已由 Gateway 团队对齐。**这是巨大的礼物，务必用它们，不要用原始波次文件重造。**
- **CHARLS** 磁盘上只有 `Harmonized_CHARLS_D.pdf`（文档），**没有对应的 `.dta`**。你必须从 `Charls/2011/*.dta` 等原始分模块文件自行构建。同理 **KLoSA** 只有 `w01_e.dta`–`w09_e.dta` 原始波次。
- **CLHLS** 本身就没有官方 harmonized 版本，必须从 `.sav` 自建。

**这意味着一个不对称的现实**：你的**开发队列（CHARLS）恰恰是最难处理的那个**，而验证队列反倒现成。

**处理策略（两条路，我推荐第一条）**：

- **推荐：先去 Gateway 官网下载 Harmonized CHARLS 的 `.dta` 文件。** 你已经有文档 PDF，说明这个产品存在（Harmonized CHARLS Version D，覆盖 2011–2018）。到 `g2aging.org` 注册后免费下载。这能省掉数周工作，且保证你的 CHARLS 变量定义与 SHARE/HRS/ELSA/MHAS 出自同一套 harmonization 逻辑——**这对本项目「测量等价性」的立论至关重要**。若 CHARLS 自建、其他队列用 Gateway，你无法排除「性能差异来自你自己的编码差异」这一致命混杂。
- 备选：全部自建。仅当 Gateway CHARLS 无法获取时采用，且必须严格按 PDF 文档的定义复刻，并做交叉核验（用 PDF 中报告的描述统计对账）。

**KLoSA 无 Gateway 版本**（Gateway 有 Harmonized KLoSA，请一并检查是否可下载；若无则自建）。自建时以英文版 `w0X_e.dta` 为准，逐波对齐。

## 4.4 各队列死亡信息的位置（已核对）

死亡结局是本项目的命脉。我核对了各队列死亡信息的实际所在：

| 队列 | 死亡信息来源 | 磁盘位置 | 关键变量（预期） |
|---|---|---|---|
| CHARLS | Exit Interview（2013）、Exit Module（2020）、各波 Sample_Infor / 家户名册存活状态 | `Charls/2013/Exit_Interview.dta`、`Charls/2020/Exit_Module.dta`、各波 `Sample_Infor.dta` | 死亡年月、死因（Verbal_Autopsy 2013） |
| CHARLS | ⚠️ **2015、2018 波次目录下未见独立 Exit 文件** | — | **需核查**：死亡状态可能藏在 `Sample_Infor.dta` 或家户名册中 |
| CLHLS | 纵向文件内置死亡日期与存活状态变量 | `CLHLS/CLHLS_dataset_2008-2018_SPSS/*.sav` | `dth**_**`、`d**` 系列（须查 codebook） |
| KLoSA | 专门的 EXIT 文件，w03–w08 | `KLOSA/w03_exit_e.dta` … `KLoSA 8th wave_EXIT/w08_exit_e.dta` | 死亡年月 |
| SHARE | Gateway EOL 文件 | `share harmonised/GH_SHARE_EOL_g.dta` | `radyear`、`radmonth` |
| HRS | Harmonized HRS 主文件内含死亡变量 | `HRS Products/harmonised HRS/H_HRS_d.dta` | `radyear`、`radmonth`、`raddate` |
| ELSA | 专门的 EOL harmonized 文件 | `ELSA/stata/stata13_se/h_elsa_eol_a2.dta` | `radyear`、`radmonth` |
| MHAS | 专门的 EOL harmonized 文件 | `MHAS/H_MHAS_EOL_b.dta` | `radyear`、`radmonth` |

**Phase 1 的第一个任务就是逐队列确认上述变量真实存在、取值范围合理、缺失码已正确识别。** 在这一步之前，任何建模都是空中楼阁。若某队列的死亡日期只到年份（无月份），必须在方案中规定统一的处理规则（如统一取该年 7 月 1 日，或采用区间删失方法），并作为敏感性分析项。

## 4.5 修订后的队列角色分配

综合 4.2–4.4，我建议的最终设计如下（这是对原方案的实质性修订，请确认）：

```
                        ┌──────────────────────────────┐
     Aim 1              │  开发：CHARLS (中国, 45+)     │
     中国内部            │  外部验证：CLHLS (中国, 65+)  │
                        └──────────────────────────────┘
                                      ↓
                        ┌──────────────────────────────┐
     Aim 2              │  LOCO 3 轮，亚洲三队列轮转     │
     亚洲多样性          │  CHARLS / CLHLS / KLoSA      │
                        │  + 单队列 vs 多队列对照        │
                        │  + 降采样对照（控制样本量）     │
                        └──────────────────────────────┘
                                      ↓
                        ┌──────────────────────────────┐
     Aim 3              │  亚洲池开发 →                 │
     跨文化迁移          │  SHARE (欧洲 27 国)           │
                        │  HRS   (美国)                │
                        │  MHAS  (墨西哥, 中高收入) ★    │
                        │  + SHARE 内 leave-one-country │
                        └──────────────────────────────┘

★ = 相对原方案新增，提升跨收入水平论证
JSTAR = 移出主设计，列为 conditional cohort
CHNS  = 不纳入（非老龄化专门队列，年龄结构与随访设计不匹配）
```

**关于 CHNS**：磁盘上有 1.1 GB，但 CHNS 是营养与健康调查，非老龄化专门队列，年龄下限低、死亡随访不是设计重点。C17 曾用它做 FI 工具开发，但那是横断面 FI 评估而非死亡预测。建议不纳入，避免削弱队列可比性论证。

---

# 第 5 章 研究问题、假设与设计骨架

## 5.1 三个 Aim 的问题陈述

写论文时，每个 Aim 都要能用一句话说清「问什么、用什么数据答、成功的判据是什么」。

**Aim 1 — 中国内部稳定性**

> 在 CHARLS 中开发的 4 年全因死亡预测模型，冻结后应用于 CLHLS，判别、校准与临床效用是否保持？

- 为什么这个问题不 trivial：CHARLS 与 CLHLS 都是中国队列，但**抽样框架截然不同**。CHARLS 是 45 岁以上全国概率抽样，代表一般中老年人群；CLHLS 刻意超额抽样高龄与百岁老人，年龄结构极度右偏（大量 80–100+ 岁）。这构成一次**在同一国家内部、但人群谱系差异极大**的迁移测试。如果模型在这里就崩，说明它对年龄结构极度敏感，后续跨国验证的解释会完全不同。
- 成功判据（预先规定）：外部 C-index ≥ 0.70，且校准斜率在 0.8–1.2 之间。**注意：这不是「通过/失败」的门槛，而是解释的锚点。** 达不到也是一个有价值的、可发表的结果，前提是能把原因分解清楚。

**Aim 2 — 亚洲多样性是否改善迁移**

> 在亚洲开发池中增加队列多样性（从单一队列到多队列），能否提升对未见过的亚洲队列的迁移性能？

- 设计：leave-one-cohort-out（LOCO），三轮轮转。
- **必须做的对照**：单队列开发 vs 多队列开发，在**同一个留出队列**上比较。否则你无法区分「性能提升来自多样性」还是「来自样本量变大」。
- **降采样对照（关键，多数论文漏掉）**：把多队列池降采样到与单队列相同的样本量与事件数，再比较。这样才能真正把「多样性效应」与「样本量效应」分离开。这是本项目的一个方法学卖点。
- 成功判据：多队列开发在留出队列上的 C-index 与校准优于单队列开发，且降采样后优势依然存在。

**Aim 3 — 跨文化迁移**

> 亚洲池上开发的模型迁移到欧洲（SHARE）、美国（HRS）、墨西哥（MHAS）时，性能如何变化，变化能归因于哪些机制？

- 四个目标队列**分别独立验证**，不合并。合并会掩盖国家间异质性。
- SHARE 内部额外做 **leave-one-country-out**：SHARE 覆盖 27 国，这让你能在单一 harmonization 框架内考察国家层面异质性，是本项目独有的机会。SHARE 内不同国家共用同一问卷与同一 harmonization，所以国家间差异不可能来自测量差异——这是一个**天然的对照实验**，能把「文化/制度差异」与「测量差异」分离。
- **结论表述的红线**：只能说「对欧洲、美国、英国、墨西哥队列的外部验证」，**不能说「全球可推广」**。缺席的是撒哈拉以南非洲、南亚、中东、东南亚。审稿人一定会抓这一点。

## 5.2 三个 Aim 之间的逻辑递进

不要把三个 Aim 写成三个独立实验。它们必须是一条论证链：

1. Aim 1 回答「模型对**人群结构**变化敏感吗」（同国家、不同年龄谱）。
2. Aim 2 回答「训练数据的**多样性**能否作为改善迁移的杠杆」（同文化圈、不同国家）。
3. Aim 3 回答「跨越**文化与医疗制度**边界后，模型还剩多少，剩下的是什么」（跨大洲、跨收入水平）。

论文的 Discussion 应当围绕「随着迁移距离增加，判别损失与校准损失的**不对称性**」展开。这是本研究最可能的核心发现——文献中（C02、C19）反复出现「判别中度下降、校准严重失准」的模式。若你能系统地量化并解释这个不对称，就有了 Nature Aging / Lancet Healthy Longevity 级别的故事。

## 5.3 预先注册的假设

预先写下假设，并在结果出来后诚实对照。这既是科学规范，也是审稿人信任的来源。

| 编号 | 假设 | 预期方向 | 依据 |
|---|---|---|---|
| H1 | FI 对 4 年死亡有稳健预测价值，在所有队列中方向一致 | FI ↑ → 死亡风险 ↑ | C08、C09、C10 一致证据 |
| H2 | 判别（C-index）跨队列下降幅度中等（约 0.03–0.08） | 轻中度下降 | C02（0.934→0.792）、C03（0.799→0.770/0.821）、C19（0.797→0.695） |
| H3 | **校准的失准比判别的下降更严重** | 校准斜率显著偏离 1，截距系统性偏移 | C19、C07 的校准报告模式 |
| H4 | 年龄、性别、ADL、自评健康的重要性排序跨队列稳定；生活方式与社会经济变量排序不稳定 | 部分稳定 | C03 明确报告此模式 |
| H5 | 增加开发池多样性可改善迁移，但**降采样后效应减弱** | 效应存在但部分由样本量驱动 | 无直接文献，本研究原创贡献点 |
| H6 | IC 五域的跨文化测量等价性弱于 FI | IC 需重校准或降级为并行模块 | C12（潜在状态跨国差异）、C11 |
| H7 | 目标队列仅做截距重校准即可恢复大部分临床效用，无需重新拟合模型 | 截距校准足够 | C07、临床预测模型文献通例 |

**H3 与 H5 是本研究最有原创价值的两个假设。** H5 尤其重要，因为它是本项目在方法学上真正「新」的部分——「训练数据多样性 vs 样本量」的因果分离，在老龄化预测文献中尚未被严格做过。

## 5.4 这个设计的创新性在哪里（诚实评估）

你的第一篇论文需要清楚知道自己的贡献边界。我按强弱排序：

**真正的创新（可作为主要卖点）**：

1. **降采样对照下的「多样性 vs 样本量」分离**（H5）。这是方法学原创。C03、C07 等多队列研究都没有做这个对照。
2. **SHARE 内 leave-one-country-out 作为「测量已对齐、仅文化/制度不同」的天然对照**。这能把迁移失败的原因拆开，是很少有人利用的设计。
3. **7 队列 × 4 大洲 × 跨收入水平的系统性 source-target 性能矩阵**，配完整的漂移诊断（SMD/PSI）+ 校准阶梯。规模与系统性超过现有文献（C03 为 3 队列、C07 为 5 队列但非 ML、C18/C19 为专病）。

**中等强度（支撑性贡献）**：

4. FI 与 IC 两套老年表型在同一迁移框架下的直接对比。文献中 FI 与 IC 研究几乎是两个互不往来的社群。
5. 判别损失与校准损失的不对称性量化。

**不是创新（不要作为卖点，写进去会被审稿人反击）**：

6. ❌ 「我们比较了 N 个机器学习算法」——C16 的 meta-analysis 已明确指出这没有价值。
7. ❌ 「我们用 SHAP 做了可解释性」——2026 年这是标配，不是贡献。
8. ❌ 「我们的 AUC 达到 0.9」——单队列高 AUC 在本项目框架下几乎无意义。
9. ❌ 「我们开发了在线预测工具」——在跨文化校准尚未解决前推工具，是审稿人最反感的过度声称。**本项目第一篇论文明确不做在线工具。**

---

# 第 6 章 数据资源实际盘点（本次亲自核查的结果）

这一章是我实际列目录看到的东西，不是从文献里抄的。**这是全书最重要的一章**，因为它决定了你的设计能不能真的跑起来。

## 6.1 磁盘上真实存在的数据

根目录：`D:/AI_project/sql/`（合计约 18 GB）

| 队列 | 目录 | 体积 | 实际内容 | Harmonized 版本 |
|---|---|---|---|---|
| CHARLS | `Charls/` | 831 MB | 原始波次 2011 / 2013 / 2015 / 2018 / 2020，各含分模块 `.dta` | ❌ **只有文档 PDF，无 H_CHARLS 数据文件** |
| CLHLS | `CLHLS/` | 498 MB | 1998–2005 SPSS、2008–2018 SPSS 纵向文件、2018 横断面、codebook、问卷 | ❌ 无（Gateway 无 CLHLS） |
| KLoSA | `KLOSA/` | 1.2 GB | w01–w09 原始 `.dta`（STATA + EXCEL），w03–w08 EXIT 文件 | ❌ **无 H_KLOSA 文件** |
| SHARE | `SHARE/` | 6.2 GB | wave 1–9 全模块 STATA + **easySHARE rel9.0.0** | ✅ 见下一行 |
| SHARE (Gateway) | `share harmonised/` | 2.0 GB | `GH_SHARE_g.dta`、`GH_SHARE_EOL_g.dta`、LH、COVID + 4 份文档 | ✅ **有** |
| HRS | `HRS Products/` | 2.8 GB | `harmonised HRS/H_HRS_d.dta` + RAND Fat Files 1992–2020 | ✅ **有** |
| ELSA | `ELSA/` | 3.9 GB | `stata/stata13_se/h_elsa_g3.dta` + `h_elsa_eol_a2.dta` + 原始波次 | ✅ **有** |
| MHAS | `MHAS/` | 179 MB | `H_MHAS_c2.dta` + `H_MHAS_EOL_b.dta` + 文档 | ✅ **有** |
| CHNS | `CHNS/` | 1.1 GB | 原始 + 已清洗数据 + 数据说明 | ❌ 无（非老龄专用队列） |
| **JSTAR** | — | — | **完全不存在** | — |

根目录另有三份关键文档 PDF，**开工前必读**：
- `Harmonized_CHARLS_D.pdf`（2.7 MB）
- `Harmonized HRS D 1992-2021.pdf`（6.2 MB）
- `Harmonized ELSA Documentation, Version G.3 (2002-2019), June 2023.pdf`（5.5 MB）

## 6.2 🔴 关键发现一：JSTAR 不存在

原始设计（`AGENTS.md` 与 codex 报告的 Aim 2）依赖 JSTAR，但 `D:/AI_project/sql/` 下**没有 JSTAR 目录**，Aim 2 的「双向 leave-one-cohort-out（KLoSA / JSTAR 轮转）」**当前无法执行**。

JSTAR 的现实情况也需要你知道：它由日本 RIETI 管理，需单独申请，覆盖城市数量有限（5–10 市），样本量约 4,000–8,000，且**随访死亡确认较弱**——即使拿到，作为死亡结局的验证队列，事件数很可能不足。

**我的建议：放弃 JSTAR，改用你已经拥有的 MHAS。** 理由：

1. MHAS 有现成的 Gateway harmonized 文件与专门的 End-of-Life 模块，死亡结局质量高于 JSTAR。
2. MHAS（墨西哥）引入**中等收入国家**背景，这在可推广性论证上比再加一个高收入东亚国家**强得多**。审稿人会问「你的模型能用在中低收入国家吗」，MHAS 是唯一能部分回答的队列。
3. 数据立即可用，不需要等数月的申请周期。
4. 6 个队列（CHARLS、CLHLS、KLoSA、SHARE、HRS、MHAS）已经是这一文献领域中队列数最多的 ML 迁移研究之一。

**修订后的三 Aim 结构**：

| Aim | 开发池 | 验证目标 | 迁移距离 |
|---|---|---|---|
| Aim 1 | CHARLS | CLHLS | 同国家，不同年龄谱 |
| Aim 2 | {CHARLS} / {CHARLS+CLHLS} / {CHARLS+CLHLS+KLoSA} 轮转 | LOCO 留出队列 | 同文化圈（东亚） |
| Aim 3 | CHARLS+CLHLS+KLoSA（亚洲池） | SHARE、HRS、MHAS **各自独立**（共3目标队列）+ SHARE 内 leave-one-country-out | 跨大洲、跨收入水平 |

## 6.3 🔴 关键发现二：CHARLS 与 KLoSA 没有 Harmonized 文件

这是**工作量分布的核心事实**，会直接决定你的时间表：

- SHARE、HRS、MHAS：有 Gateway/RAND harmonized 文件。变量名统一（如 `r{wave}agey`、`r{wave}shlt`、`r{wave}adla`），跨队列几乎可以直接对齐。**每个队列的变量构建约 3–5 天。**
- CHARLS、KLoSA、CLHLS：**必须从原始模块手工构建**。要自己合并 `Demographic_Background.dta` + `Health_Status_and_Functioning.dta` + `Cognition.dta` + `Exit_Interview.dta` 等，自己处理跳转逻辑、缺失码（CHARLS 常用 `.m`/`.d`/`.r`，KLoSA 用 `-9`/`-8`/`-1`，CLHLS 用 `8`/`9`/`88`/`99`）、自己对齐波次。**每个队列约 2–3 周。**

**这意味着一件很反直觉的事：你的开发队列（CHARLS）恰好是最难构建的那一个。** 全项目约 **60–70% 的数据工程量集中在 CHARLS + CLHLS + KLoSA 这三个亚洲队列上**，而它们只占总数据体积的 14%。

排期上必须据此调整：**先啃 CHARLS，不要先做看起来简单的 SHARE。** 因为 CHARLS 的变量字典决定了所有其他队列要对齐到什么，先做 SHARE 会导致返工。

## 6.4 各队列死亡信息的具体位置（已核查）

死亡结局是本项目的命门。以下是我实际找到的文件：

| 队列 | 死亡信息来源 | 核查状态 |
|---|---|---|
| CHARLS | `2013/Exit_Interview.dta`、`2013/Verbal_Autopsy.dta`、`2020/Exit_Module.dta` | ✅ 已确认存在。⚠️ **2015 与 2018 波次目录下没有独立 Exit 文件**——2015/2018 的死亡需从 `Sample_Infor.dta` 的随访状态字段或家户名册变化推断。**这是第一个必须解决的技术问题。** |
| CLHLS | `clhls_2008_2018_longitudinal_dataset_released_version1.sav` 等纵向文件内含死亡日期与随访状态变量 | ✅ 存在，需查 codebook 确认变量名（通常为 `dth**` 系列 + `monthin`/`yearin`） |
| KLoSA | `w03_exit_e.dta`、`w04_exit_e.dta`、`w05_exit_e.dta`、`w06_Exit_e.dta`、`2018 KLoSA 7 wave EXIT/w07_exit_e.dta`、`KLoSA 8th wave_EXIT/w08_exit_e.dta` | ✅ w03–w08 齐全。⚠️ **w01、w02、w09 无 EXIT 文件**，需确认 w09 死亡如何获取 |
| HRS | `H_HRS_d.dta` 内 `RADYEAR`/`RADMONTH`（RAND 死亡日期），另可交叉核对 RAND Fat Files | ✅ 高质量，含 NDI 链接 |
| ELSA | `h_elsa_eol_a2.dta`（End-of-Life 模块）+ `h_elsa_g3.dta` 状态变量 | ✅ 有专门 EOL 文件 |
| SHARE | `GH_SHARE_EOL_g.dta`（End-of-Life）+ `GH_SHARE_g.dta` | ✅ 有专门 EOL 文件 |
| MHAS | `H_MHAS_EOL_b.dta` + `H_MHAS_c2.dta` | ✅ 有专门 EOL 文件 |

**注意 easySHARE 的陷阱**：`easySHARE_rel9-0-0.dta` 用起来最方便，但它是**简化教学版**，变量数远少于完整版，FI 的 40+ deficits **几乎肯定无法从 easySHARE 单独构建**。easySHARE 可用于快速探索与描述性分析，但主分析应用 `GH_SHARE_g.dta` 或完整波次文件。这一点容易踩坑，因为 easySHARE 太好用了。

## 6.5 各队列基线波次与随访窗口建议

4 年主结局需要基线后至少 4 年的随访。以下是 6 个队列的基线选择与实际采用的时间框架（已实测，2026-07-29）：

| 队列 | 基线 | 4 年终点 | 7 年终点 | 随访来源波次 | 备注 |
|---|---|---|---|---|---|
| CHARLS | 2011 (wave 1) | ~2015 | ~2018 | 2013 / 2015 / 2018 | 离散时间，D-010；2020 波为探索性约 9 年 |
| CLHLS | 2011/2012 | 基线 +1461 天 | 基线 +2557 天 | 2011–2018 纵向文件 | 有精确死亡日期（d14vyear/d18vyear），D-019 |
| KLoSA | 2012 (w4) | 2016 | 2019 | w05–w08 + EXIT 文件 | 建议 **2012**，EXIT 覆盖 w05–w08 完整 |
| SHARE | 2011 (wave 4，仅 r4iwy==2011) | 2015 | 2018 | wave 5–9 + EOL | D-021：仅选2011访谈，n=54,550 |
| HRS | 2012 | 2016 | 2019 | 2014–2020 + RAND 死亡日期 | 随访最完整 |

| MHAS | 2012 | 2016 | 2019 | 2015 / 2018 + EOL | D-022：年龄直接取 r3agey 或从出生年推算 |

**为什么建议尽量把基线对齐到 2011–2012**：如果各队列基线年份差异过大（比如 CHARLS 用 2011、HRS 用 2004），那么「跨队列性能差异」就会混入「时期效应」（医疗水平、死亡率长期下降趋势）。对齐基线年份能消除这个混淆因素。这是审稿人会检查的细节。

**MHAS 的 3 年波次间隔**：MHAS 在 2012 后是 2015、2018，没有精确到年内的中间波次。4 年死亡（2012→2016）需要用 EOL 模块的**具体死亡日期**来判定，而不能只用「哪一波次失访」。这也说明为什么 EOL 文件比波次状态更可靠。

## 6.6 开工前必须完成的 7 个数据核查任务

在写任何建模代码之前，先做完这 7 件事。每一件都会暴露可能颠覆设计的问题，**越早发现越省时间**。

| # | 任务 | 具体操作 | 通过标准 |
|---|---|---|---|
| 1 | CHARLS 2015/2018 死亡认定 | 读 `2015/Sample_Infor.dta`、`2018/Sample_Infor.dta`，找随访状态字段；对照 `2013/Exit_Interview.dta` 与 `2020/Exit_Module.dta` 的 ID | 能为每个 2011 基线个体给出「死亡 / 存活 / 失访」三分类 + 死亡年月 |
| 2 | 各队列事件数清点 | 按 6.5 的基线设定，统计 4 年内死亡数 | **每队列 ≥ 100 死亡事件**（外部验证最低要求），开发队列 ≥ 500 |
| 3 | FI 变量可得性矩阵 | 对 45 个候选 deficit，逐队列标记「可得 / 部分可得 / 缺失」 | 每队列 ≥ 40 项可得；所有队列共同项 ≥ 30 项 |
| 4 | 缺失码清单 | 逐队列列出所有特殊缺失码及其含义 | 有一份书面对照表，无「-9 被当成年龄 -9 岁」这类事故 |
| 5 | 随访时间分布 | 画各队列随访时间直方图 + Kaplan-Meier 曲线 | 无异常截断，无大量 0 天随访 |
| 6 | 失访率 | 计算 4 年内失访（非死亡）比例 | < 20% 理想；> 30% 需在论文中专门讨论并做敏感性分析 |
| 7 | easySHARE vs 完整 SHARE 对比 | 检查 FI 的 40 项在 easySHARE 中能构建多少 | 明确记录 easySHARE 不足以构建 FI（预期结论） |

**做这 7 项时的一个习惯**：每一项都输出一个小的 CSV 或 Markdown 表格存到 `results/data_audit/`，并写一句话结论。这些表格后来会直接变成论文的 Supplementary Table。不要只在终端里看一眼就过。

---

# 第 7 章 研究人群与结局定义

## 7.1 纳入标准

统一适用于所有 6 个队列：

1. 基线波次年龄 **≥ 60 岁**（见下方讨论）
2. 基线时**存活**且完成个人访问
3. FI 可计算：至少 **80% 的 deficit 项目非缺失**（即 40 项中至少 32 项有值）
4. 有可用的随访状态信息（能判定死亡或存活）

## 7.2 排除标准

1. 基线年龄缺失或明显异常（< 45 或 > 115）
2. FI 缺失项 > 20%
3. 基线后随访时间 = 0（同一天死亡/退出，无法定义时间）
4. 代理访问（proxy interview）为唯一信息来源者 → **单独讨论，见下**

## 7.3 关于年龄下限的决策

CLHLS 是**长寿队列**，设计上偏重 80 岁以上，65 岁以下样本极少；CHARLS 从 45 岁起访；SHARE/HRS/MHAS 从 50 岁起访。

**建议：主分析用 ≥ 60 岁**，理由是这是所有 6 个队列都有实质样本的重叠区间，也是老年医学的常规界值。

**但必须做一个预先规定的敏感性分析**：CHARLS→CLHLS 的年龄谱差异极大（CHARLS 60+ 中位年龄约 67，CLHLS 60+ 中位年龄可能 80+）。这个差异本身就是「迁移失败」的一个主要来源。所以要额外做：

- **敏感性分析 A**：限制到 65–85 岁的共同重叠区间，重跑 Aim 1。如果性能明显改善，说明「性能下降主要来自年龄谱不匹配」而非「模型本身不可迁移」——这是一个有价值的发现，而不是失败。
- **敏感性分析 B**：按年龄段（60–69、70–79、80+）分层报告 C-index 与校准，展示模型在哪个年龄段可迁移、哪个不行。

这个设计上的自觉会成为你论文的一个亮点。多数同类研究只报告一个总体 AUC 下降，然后含糊地归因于「人群差异」。你如果能把下降**分解**到年龄谱、事件率、变量分布三个来源，方法学贡献立刻上一个层级。

## 7.4 代理访问（proxy interview）的处理

老龄队列中，认知严重受损或身体状况极差的受访者由家属代答。这些人**恰恰是死亡风险最高的**。

- 如果全部排除 proxy：会系统性剔除高危人群，导致模型低估风险，且各队列 proxy 比例不同（HRS 较高，CHARLS 较低）会引入不可比性。
- 如果全部纳入：某些变量（如自评健康、抑郁量表）由代理回答的效度与自答不同。

**建议**：纳入 proxy，但（a）加一个 `proxy_flag` 变量，（b）预先规定一个「仅自答者」的敏感性分析，（c）在 Table 1 中报告各队列 proxy 比例。这是诚实且可辩护的做法。

## 7.5 主结局：4 年全因死亡（v2.0 修订，D-010）

**定义**：基线（2011/2012）后至第 3 个调查波次（约 2015/2016），即**风险期 1 + 风险期 2**的全因死亡。

**为什么是 4 年而不是 5 年**（D-010 完整依据见 `docs/charls_outcome_plan_v2_2026-07-27.md`）：

CHARLS 2015/2018 波次没有死亡日期——`Exit_Interview.dta` 仅存在于 2013 波（431 行），`Exit_Module.dta`（2020 波）只有 107/770 条有日期。Gateway 官方程序（`bbxleyec.do` 第 3316 行）亦只覆盖 `inrange(exb001_1,2011,2014)`。在无法计算"第 N 天死亡"的情况下，**强行插补日期比离散时间更坏**。

CHARLS 波次结构（2011→2013→2015→2018→2020）产生 4 个风险期，"期 1+2"的实际跨度恰好约 4 年。没有"恰好 5 年"的切点，名义上写"5 年"而实际用"4 年"是名实不符。

**离散时间框架**：

```
风险期  间隔          死亡认定来源
期 1    2011→2013    2013/Exit_Interview.dta
期 2    2013→2015    2015/Sample_Infor.died==1
期 3    2015→2018    2018/Sample_Infor.died==1
期 4    2018→2020    2020/Sample_Infor.died==1
```

主结局 = 期 1+2 内死亡（event==1 且 period≤2）。

**主分析模型**：

```
logit P(死于第 k 期 | 存活至 k) = α_k + β'X      (pooled logistic)
cloglog P(死于第 k 期 | 存活至 k) = α_k + β'X    (complementary log-log，β ≈ 对数风险比)
```

两个模型并列报告。连续时间 Cox 降为**敏感性分析**（仅在有精确死亡日期的队列：CLHLS / HRS / SHARE / MHAS）。

**论文 Methods 标准表述**：

> The primary outcome was all-cause mortality within the first two follow-up intervals after baseline (2011 to approximately 2015, nominally 4 years). Because deaths in CHARLS from 2015 onward are recorded only at the wave level without exact dates, follow-up time was modelled as discrete wave-based intervals; the 4-year window corresponds exactly to the observed interval structure rather than to an imposed calendar cut-off.

**事件数（已实测，2026-07-28）**：

| 队列 | 4 年事件（60+） | 累计死亡率（60+） |
|---|---|---|
| CHARLS（开发） | **785** | 10.24% |
| CLHLS | **3,502** | 35.9% |
| KLoSA | **585** | — |
| SHARE | **3,689** | D-021 applied |
| HRS | **2,352** | 16.96% |
| MHAS | **1,404** | D-022 applied |

**CHARLS ID 桥接规则**（D-009，务必写进 Methods）：

```python
pid_12 = householdID_11 + "0" + id_11[-2:]
```

CHARLS 2011 波的 ID 为 11 位，后续波次为 12 位，直接匹配重叠为 0。官方规则来自 `bbxleyec.do` 第 1918–1919 行，已实测匹配率 85.5%/81.3%/77.6%/94.7%（见 D-009）。

## 7.6 次要结局（v2.0 修订）

| 结局 | 定义 | 用途 |
|---|---|---|
| **约 7 年死亡** | 期 1+2+3（2011→2018，实际约 7 年） | 时间窗稳定性检验（替代原"8 年"，D-010） |
| Incident ADL 失能 | 基线独立，4 年内出现 ≥1 项依赖 | 功能结局的跨文化迁移 |

原"8 年全因死亡"改为"约 7 年"，因为 CHARLS 最大波次跨度为 2011→2018（4 个风险期约 9 年），CLHLS 最大跨度约 7 年；**两队列共同可行的最长时间窗是约 7 年**。

## 7.7 时间精度不对称（v2.0 新增，需在论文中声明）

| | CHARLS（开发集） | 其余 5 个验证集 |
|---|---|---|
| 死亡日期 | 波次级（2015+ 无精确日期） | 多数有精确年月 |
| 4 年实现 | 期 1+2 波次近似 | 可用日期精确切分 |

这不影响外部验证的有效性（模型输入是基线预测因子，4 年死亡结局在实质上一致），但 Methods 必须说明，并做一项敏感性分析：在有精确日期的队列上**也用波次近似**，检验结论是否一致。

---

# 第 8 章 变量 Harmonization：本项目最关键的一步

如果这一章做得草率，后面所有统计分析都是无意义的。请把这一章当作整个项目的地基。

## 8.1 为什么 harmonization 是硬骨头

七个队列问的不是同一个问题。举一个真实的例子：

| 队列 | 抑郁测量 | 题目数 | 计分 | 常用界值 |
|---|---|---|---|---|
| CHARLS | CES-D-10 | 10 | 0–30 | ≥ 10 或 ≥ 12 |
| CLHLS | 自编情绪题 | 7–8 | 各题 1–5 | 无统一界值 |
| HRS | CES-D-8 | 8 | 0–8 | ≥ 3 或 ≥ 4 |
| ELSA | CES-D-8 | 8 | 0–8 | ≥ 3 |
| SHARE | EURO-D | 12 | 0–12 | ≥ 4 |
| MHAS | CES-D-9 | 9 | 0–9 | ≥ 5 |
| KLoSA | CES-D-10 | 10 | 0–10 | ≥ 4 |

直接把「抑郁得分」当作同一个变量放进模型，是错的。CES-D-10 的 8 分和 EURO-D 的 8 分含义完全不同。

**本项目的解法**：不用连续分数，而是用**各量表自己已验证的界值**转成二分类「抑郁症状阳性 = 1」，作为 FI 的一个 deficit。这牺牲了信息量，但换来了跨队列可比性。这是一个明确的、可辩护的权衡，要在 Discussion 的 limitation 中写出来。

## 8.2 Harmonization Table 的强制格式

建立一个 Excel/CSV，**每一行是「一个概念 × 一个队列」**，也就是 41 个 deficit 概念 × 6 个队列 ≈ 246 行。必需列：

| 列名 | 说明 | 示例 |
|---|---|---|
| `concept_id` | 概念编号 | `FI_D07` |
| `concept_name` | 概念名 | 糖尿病 |
| `domain` | 所属域 | 共病 |
| `used_in` | 用于 FI / IC / 协变量 | FI |
| `cohort` | 队列 | CHARLS |
| `wave` | 波次 | 2011 |
| `source_file` | 文件名 | `health_status_and_functioning.dta` |
| `source_var` | 原始变量名 | `da007_1_` |
| `question_text_orig` | 原始题目（原语言） | 医生是否告诉您患有糖尿病或血糖高 |
| `question_text_en` | 英文题目 | Have you been told by a doctor... |
| `value_labels` | 取值含义 | 1=Yes, 2=No |
| `missing_codes` | 缺失码 | .m, .d, .r, 8, 9 |
| `recode_rule` | 编码规则（**可执行伪代码**） | `1→1; 2→0; else→NA` |
| `deficit_value` | 转成 deficit 后的值 | 0 / 0.5 / 1 |
| `availability` | 该队列是否可得 | 可得 / 部分 / 不可得 |
| `coverage_pct` | 非缺失比例 | 98.7% |
| `equivalence_note` | 可比性备注 | 各队列均为自报医生诊断，可比性中等 |
| `verified_by` | 核对人 + 日期 | 你 2026-07-30 |

**关键纪律**：`recode_rule` 必须写得能直接翻译成代码。写「recode 成 0/1」是无用的；写 `1→1; 2→0; 3→NA; 9→NA` 才是可执行的。代码里的 recode 必须与这一列**逐字对应**，任何不一致都是 bug。

## 8.3 缺失码陷阱（新手最容易栽的地方）

Stata/SPSS 文件里的缺失通常不是空值，而是特殊数字或扩展缺失码：

- CHARLS：`.m`（不知道）、`.d`（拒答）、`.r`、`.s` 等 Stata 扩展缺失
- CLHLS（SPSS）：常用 `8`、`9`、`88`、`99`、`-8`、`-9` 表示不适用/不知道
- HRS RAND：`.M`、`.D`、`.R`、`.N`、`.X` 等
- SHARE：`-1`（不知道）、`-2`（拒答）、`-9`（不适用）、`-15`、`-16` 等

如果你不显式处理，**`9` 会被当作一个真实的数值 9 参与计算**。一个「9 = 不知道」的变量被当成 9 分严重程度，会彻底毁掉 FI。

**强制检查**：对每个变量输出频数表（含所有取值），人工看一眼再写 recode 规则。这一步没有捷径，也不能让 AI 代替你「猜」。约 315 行的 harmonization table，每行都要有人眼看过一次频数表。

## 8.4 FI 的构建规范

**候选清单 45 项**（分域列出，最终以能在 ≥5 个队列构建为准）：

**共病（10 项）**：高血压、糖尿病、心脏病、脑卒中、癌症、慢性肺病、肝病、肾病、消化系统疾病、关节炎

**ADL（6 项）**：穿衣、洗澡、进食、上下床、如厕、大小便控制

**IADL（5 项）**：做家务、做饭、购物、管钱、服药

**活动能力（8 项）**：走 100 米、爬一层楼、久坐后起立、蹲下/跪下、举手过肩、提 5 公斤、从桌上拿硬币、行走 1 公里

**感觉（3 项）**：视力差、听力差、咀嚼/牙齿问题

**心理认知（5 项）**：抑郁症状阳性、记忆自评差、认知测试低分、失眠、生活满意度低

**一般健康（8 项）**：自评健康差、疼痛、跌倒史、尿失禁、疲乏、体重下降、BMI 异常（<18.5 或 ≥30）、住院史

**deficit 编码规则**（Searle 等 2008 的标准做法）：
- 二分类：有 = 1，无 = 0
- 三分类：无 = 0，中度 = 0.5，重度 = 1
- 五分类自评健康：优 = 0，很好 = 0.25，好 = 0.5，一般 = 0.75，差 = 1

**FI 计算**：`FI = 已有 deficit 之和 / 非缺失 deficit 项目数`，取值 0–1。要求非缺失项 ≥ 32 项（80%）。

**必须预先冻结并记录**：
1. 最终纳入的 deficit 清单（在看到任何结局数据之前锁定）
2. 每个队列的实际可用项目数（写进论文 Table）
3. 分母规则（用非缺失数作分母，不用固定 40）
4. **共同核心 FI**：只用所有 6 个队列都能构建的项目（预计 27–35 项，MHAS 约束为 27）另算一个 `FI_core`，作为主分析的敏感性对照。如果 `FI_core` 的迁移性能与全项 FI 接近，说明结论对 FI 构成不敏感——这是一个强有力的稳健性证据。

## 8.5 IC 五域的构建规范

| 域 | 测量内容 | 各队列常见来源 | 主要风险 |
|---|---|---|---|
| Locomotion 运动 | 步速、起立测试、握力、行走能力自评 | 体检模块（CHARLS 有握力/步速；SHARE 有握力；HRS 部分波次） | **体检变量覆盖率低**，是最大障碍 |
| Cognition 认知 | 词语回忆、定向、连续减 7、画图 | 认知模块 | 题目数与难度不同，需 DIF 检验 |
| Vitality 活力 | BMI、体重下降、疲乏、白蛋白/血红蛋白 | 体检 + 自报 | 生物标志物多数队列缺失 |
| Sensory 感觉 | 视力、听力自评 | 健康模块 | 自评题相对可比 |
| Psychology 心理 | 抑郁量表、生活满意度 | 抑郁模块 | 量表不同（见 8.1） |

**处理策略**（按优先级）：
1. **先做可得性盘点**。如果某域在 ≥2 个队列缺失率 > 30%，该域不能进主模型。
2. **各域内部标准化到 0–100**（Lee 等的做法），而不是 z 分数——因为 z 分数依赖队列均值，无法迁移。用**理论最小/最大值**做 min-max 标准化，或用**开发集的分布参数**固定下来应用到目标集。
3. **IC 总分 = 五域均值**（等权），但必须报告各域单独结果。
4. **测量等价性检验**：对认知与心理两域做 CFA 的多组不变性检验（configural → metric → scalar），或 IRT 的 DIF 检验。**若 scalar invariance 不成立**（很可能不成立），则 IC 总分不能进主模型，只能作为域别的并行模块与敏感性分析。这个结论本身就值得报告。

**诚实的预判**：以你手上的数据，IC 五域很可能无法在全部 6 个队列可靠构建（握力/步速/生物标志物的覆盖是硬约束）。**建议把 IC 定位为 Aim 1 的增量模块 + 一篇独立的第二篇论文**，不要在第一篇里强行做全队列 IC。第一篇以 FI 为主线，完整度更高、风险更低。

## 8.6 协变量集（Model A 的基础变量）

保持精简，只用跨队列稳定可得的：

- 年龄（连续）
- 性别
- 教育（转成 ISCED 或「未受教育/小学/中学/大专及以上」四类）
- 婚姻状态（有配偶/无配偶）
- 居住地（城/乡；SHARE/HRS 定义差异需注意）
- 吸烟（从不/曾经/现在）
- 饮酒（从不/偶尔/经常）
- 独居（是/否）

**不放入的**：收入与财富（跨国币值与定义不可比，且缺失率高）、具体药物、地区固定效应（会阻碍迁移）。

## 8.7 四个预先规定的模型规格

| 模型 | 变量 | 目的 |
|---|---|---|
| Model A | 协变量（8 个） | 基准：不用 FI/IC 能做到多好 |
| **Model B** | A + FI | **主模型** |
| Model C | A + IC 五域 | IC 的独立价值 |
| Model D | A + FI + IC | 增量价值 |

主结论基于 Model B。Model C/D 只有在 8.5 的等价性检验通过后才作为主要分析呈现，否则降级为敏感性分析。

---

# 第 9 章 统计分析计划（SAP）

这一章是 pre-registration 的核心。原则：**在看到任何结局关联之前把这一章写完并冻结**。

## 9.1 建模路线（v2.0 修订，D-010）

**主分析：离散时间生存模型**

```
logit P(死于第 k 期 | 存活至 k) = α_k + β'X     (pooled logistic)
cloglog P(死于第 k 期 | 存活至 k) = α_k + β'X   (complementary log-log，β ≈ ln HR)
```

- `α_k`：每期独立截距（不对基线风险作参数假设）
- `X`：基线预测因子，各期重复（预测模型，不做时变）
- 输入格式：person-period 长表（一人每期一行）
- 输出：4 年死亡概率（由各期条件概率累乘得到）

两个模型并列报告：pooled logistic 用于判别与校准，cloglog 用于给出 HR（方便与 Cox 和文献比较）。

**敏感性分析：连续时间 Cox**

仅在有精确死亡日期的队列（CLHLS / HRS / SHARE / MHAS）额外运行。同时与离散时间结果对比，量化"波次近似"带来的误差。

**放弃原路线 1（Cox 为主）的理由**：CHARLS 2015+ 无精确死亡日期，无法计算连续时间轴。开发集的方法论必须基于可行的数据，不能为了方法学上的优雅而对数据作无法验证的假设。

## 9.2 模型库（5 个，不要更多）

| 模型 | 类型 | 用于 |
|---|---|---|
| **Pooled logistic** | 广义线性 | **主分析** |
| **cloglog** | 广义线性 | 主分析（并列，给 HR） |
| Penalized logistic (LASSO) | 正则化 | 变量筛选参照 |
| Random Forest (分类) | 树集成 | 非线性基准 |
| XGBoost (分类) | 梯度提升 | 性能上限参照 |

生存版（RSF、Cox objective XGBoost）改为**敏感性分析工具**，仅在有精确日期队列上运行。

超参数必须在开发集内部用 nested CV 完成，不能用到任何目标集信息。

## 9.3 缺失数据处理（含一个致命陷阱）

**盘点**：先输出每个变量在每个队列的缺失率热图（这是补充图之一）。

**策略**：多重插补链式方程（MICE），m = 20。

**致命陷阱**：外部验证时的插补必须小心。
- ❌ **错误做法**：把开发集和目标集合并后一起插补 → 目标集信息泄漏进开发集，外部验证失效。
- ❌ **错误做法**：在目标集插补时把结局变量放进插补模型 → 结局信息泄漏。
- ✅ **正确做法**：开发集内部插补（可含结局，这是 MICE 的标准建议）；目标集**单独**插补，插补模型**不含结局变量**，只用预测变量之间的关系。
- ✅ **更保守的备选**：在目标集用开发集拟合好的插补规则（或简单用开发集的中位数/众数）填补。这更严格但会损失一些精度。两种都做，作为敏感性分析。

**FI 的缺失**：不插补 FI 本身，而是用「非缺失项占比」规则（见 8.4）。如果一个人 45 项里只答了 20 项，直接排除，不要插补出一个 FI。

## 9.4 每个 source→target 对必须报告的完整指标包

这是本项目的**结果主体**。对每一个 source→target 组合（Aim 1 有 1 对，Aim 2 有 4 对，Aim 3 有 5 对，共 10 对），报告：

**A. 判别（Discrimination）**
- Harrell C-index（含 95% CI）
- Uno C-index（对删失更稳健，审稿人会问）
- 4 年 time-dependent AUC（Uno 或 Blanche 估计量）
- CI 用 **bootstrap 1000 次**，按家庭/PSU 聚类重采样

**B. 校准（Calibration）——不要跳过这一步**
- Calibration-in-the-large（观察/预测平均风险之比，或 O:E ratio）
- Calibration slope（理想 = 1；< 1 表示预测过于极端）
- Calibration intercept（理想 = 0）
- Calibration plot：把目标队列按预测风险分 10 组，画观察 vs 预测
- Integrated Brier Score（0–4 年），并给 IPA（Index of Prediction Accuracy）

**C. 临床效用**
- Decision Curve Analysis，阈值范围预先规定为 **5%–40%**（4 年死亡风险的合理决策区间）
- 报告净获益，与「全部干预」「全不干预」两条参考线比较

**D. 迁移失败的原因诊断（本项目的创新所在）**

只报告「外部 AUC 掉了」是不够的。必须把性能下降拆解成：

| 诊断 | 方法 | 说明 |
|---|---|---|
| 事件率漂移 | 各队列 4 年死亡率对比 | 事件率不同会直接影响校准截距 |
| 协变量分布漂移 | 逐变量 SMD（标准化均差），\|SMD\| > 0.1 标注 | 哪些变量的人群分布变了 |
| 总体漂移强度 | PSI（Population Stability Index），> 0.25 视为显著漂移 | 单一数字概括漂移 |
| 测量漂移 | FI 各 deficit 的阳性率对比表 | 区分「人群真的更健康」vs「问法不同」 |
| 关系漂移 | 跨队列 SHAP 排名的 Spearman ρ + 方向一致性 | 变量与死亡的关系是否变了 |
| 概念漂移 | 在目标集重拟合模型，比较系数方向与大小 | 最强的关系漂移证据 |

**E. 重校准阶梯（Recalibration ladder）**

这是回答「模型能不能救回来」的关键，也是最实用的结论：

| 层级 | 做法 | 用了目标集的什么信息 |
|---|---|---|
| L0 | 冻结模型直接用 | 无 |
| L1 | 仅调整截距（intercept-only） | 目标集的总死亡率 |
| L2 | 调整截距 + 斜率（logistic recalibration） | 目标集的风险分布 |
| L3 | 保留变量，在目标集重拟合系数 | 目标集的全部关系 |
| L4 | 在目标集完全重新开发 | 一切 |

报告 L0→L4 每一级的性能提升。如果 L1 就把校准修好了，结论是「模型可迁移，只需本地校准」——这是非常有价值的实用结论。如果要 L3/L4 才行，结论是「关系本身跨文化不同」——这也是有价值的负面结论。

**L1–L4 必须用交叉验证或数据拆分来评估**，不能在同一份目标数据上既重校准又报告性能。

## 9.5 亚组与公平性分析

预先规定的亚组（只做这些，不做数据挖掘式的亚组搜索）：
- 年龄：60–69 / 70–79 / ≥ 80
- 性别：男 / 女
- 教育：低（小学及以下）/ 中 / 高
- 城乡
- SHARE 内部：北欧 / 西欧 / 南欧 / 中东欧（**这是一个免费的额外验证层**，SHARE 有 27 国）

每个亚组报告 C-index 与校准斜率。若某亚组严重失校准（如 ≥80 岁组），要在 limitation 明确写出，不要掩盖。

## 9.6 敏感性分析清单（预先规定 9 项，v2.0 修订）

1. **约 7 年全因死亡**（替代主结局时间窗，期 1+2+3；替代原"8 年"，D-010）
2. 连续时间 Cox（仅在有精确死亡日期的队列：CLHLS/HRS/SHARE/MHAS）
3. **在有精确日期的队列上也用波次近似**（量化时间精度不对称的影响）
4. `FI_core`（共同核心项目 FI）替代全项 FI
5. 完整病例分析（complete-case）替代 MICE
6. 目标集用开发集插补参数（替代目标集独立插补）
7. 排除基线 ADL 完全依赖者（排除临终人群）
8. CHARLS 基线改用 2013 波（检验基线波次选择的影响）
9. 竞争风险框架（若做非死亡次要结局）

## 9.7 样本量与效能（v2.0，已实测）

预测模型研究不做传统功效计算，用 Riley 等的样本量方法。**所有数字均为 2026-07-28 实测，不再是预估。**

- **开发集（CHARLS 60+）**：4 年死亡事件 **785**，Model B 约 10 个预测变量，EPV = **78**，远超 ≥20 要求。✅
- **外部验证集**：最低要求 ≥100 事件。实测全部满足：

| 队列 | 60+ 事件 | ≥100? | EPV |
|---|---|---|---|
| CLHLS | 3,502 | ✅ | 350 |
| KLoSA | 585 | ✅ | 59 |
| SHARE | 3,689 | ✅ | 369 |
| HRS | 2,352 | ✅ | 235 |
| MHAS | 1,404 | ✅ | 140 |

原"风险点：JSTAR/KLoSA 事件数可能不足"已解除。JSTAR 移出主分析（D-008）；KLoSA 实测 585 事件，EPV=59，满足 ≥20 要求。SHARE/HRS/MHAS 事件数均大幅超过最低线。（注：D-021、D-022、D-024 对事件数做了精确修正，从旧值918/6287/1735更新至585/3689/1404，见注册表2026-07-29版。）

## 9.8 计算环境与可复现性

- 语言：Python（数据处理 + ML）+ R（生存分析与校准，`survival`/`riskRegression`/`pec`/`dcurves` 生态更成熟）
- 随机种子：全局固定，写进配置文件
- 环境锁定：`requirements.txt` + `renv.lock`
- 每个分析脚本输出带时间戳的日志与结果 JSON
- **绝不修改 `D:\AI_project\sql\` 下的原始数据**，所有派生数据写到 `D:\AI_project\project3\data\derived\`

建议目录结构：

```
D:\AI_project\project3\
├── docs\                    # 本计划书、SAP、harmonization table
├── data\
│   ├── derived\             # 派生分析数据集（每队列一个）
│   └── harmonization\       # harmonization table + 频数表输出
├── code\
│   ├── 01_extract\          # 每队列一个提取脚本
│   ├── 02_harmonize\        # FI/IC/协变量构建
│   ├── 03_describe\         # Table 1、缺失率、漂移诊断
│   ├── 04_develop\          # 开发集建模
│   ├── 05_validate\         # 外部验证 + 重校准
│   └── 06_figures\
├── results\
│   ├── literature_extract\  # 已完成（Codex 输出）
│   ├── tables\
│   ├── figures\
│   └── models\              # 冻结的模型对象 + 模型卡
└── logs\
```

---

# 第 10 章 分阶段执行计划（24 周）

时间估算按你**没有医学背景、第一次做这类研究**来设定，比熟手慢约 40%。如果 Codex 承担大部分代码实现，Phase 2 可压缩。

## Phase 0：准备与冻结（第 1–2 周）

| # | 任务 | 产出 | 判定标准 |
|---|---|---|---|
| 0.1 | 读完本计划书第 2 章 + 8 篇入门文献（见第 14 章） | 读书笔记 | 能用自己的话解释 FI、校准、外部验证 |
| 0.2 | 确认数据可及性与授权状态（6 个队列） | 数据授权清单 | 每队列有明确的可用/待申请状态 |
| 0.3 | 决策：JSTAR 去留（见第 4 章） | 一句话决定，写进本文档 | 已决定 |
| 0.4 | 建立目录结构与 git 仓库 | 空骨架 | 结构就位 |
| 0.5 | 写 SAP 定稿并**冻结**（可用本计划书第 9 章直接改写） | `docs\SAP_v1.0.md` | 加时间戳、不再改动 |

**冻结的含义**：之后任何对 SAP 的修改都必须在 `docs\SAP_amendments.md` 里记录「改了什么、为什么改、在什么时间点改」。这是审稿人信任的来源。

## Phase 1：数据核验与 Harmonization（第 3–8 周，最关键、最容易被低估）

**这是整个项目的成败所在。** 新手最常见的错误是急着建模，跳过这一步，结果三个月后发现 FI 在某个队列根本构不出来。

| # | 任务 | 产出 |
|---|---|---|
| 1.1 | 逐队列盘点：基线波次、随访波次、死亡日期变量、删失日期、事件数、随访中位数 | 队列时间线表（Table S1） |
| 1.2 | **算出每队列 4 年死亡事件数**（决定谁能当验证集） | 事件数表 |
| 1.3 | 建立 Harmonization Table（45 项 FI + IC 五域 + 协变量，逐队列逐变量） | `docs\harmonization_table.xlsx` |
| 1.4 | 逐变量输出频数/分布表，人工核对编码与缺失码 | 频数表 PDF |
| 1.5 | 构建 FI，输出各队列 FI 分布、项目数、缺失分布 | FI 诊断报告 |
| 1.6 | 构建 IC 五域，做可比性检查（CFA/DIF 或域别可比性） | IC 诊断报告 |
| 1.7 | 冻结 `FI_full` / `FI_core` / IC 定义 | 变量字典 v1.0 |

**Phase 1 出口判定（Gate 1）**：
- [ ] 每个队列 4 年死亡事件数已确认，验证集事件数 ≥ 100（见注册表 2026-07-29）✅
- [ ] FI 在每个队列至少 40 项可构建（或已决定降级为 `FI_core` 方案）
- [ ] IC 五域可比性检查已完成，并已决定 IC 是主模块还是敏感性模块
- [ ] 变量字典冻结

**没通过 Gate 1 不要进入建模。** 宁可在这里多花两周。

## Phase 2：描述与开发（第 9–13 周）

| # | 任务 | 产出 |
|---|---|---|
| 2.1 | 样本流程图（每队列排除步骤与人数） | Figure 2 |
| 2.2 | Table 1：基线特征跨队列对比 | Table 1 |
| 2.3 | 缺失率热图、FI/IC 分布图 | 补充图 |
| 2.4 | CHARLS 开发：Cox 基准 → 4 个 ML，nested CV | 内部性能表 |
| 2.5 | Model A/B/C/D 增量比较 | 增量表 |
| 2.6 | **冻结模型**（序列化保存 + 模型卡） | `results\models\` |

**Gate 2**：模型冻结后，写一份「模型卡」记录：变量清单、预处理步骤、超参数、随机种子、训练样本量、内部性能。**之后不允许因为外部验证结果不好而回来改开发集。**

## Phase 3：外部验证（第 14–19 周）

| # | 任务 | 产出 |
|---|---|---|
| 3.1 | Aim 1：CHARLS → CLHLS | 完整指标包 |
| 3.2 | Aim 2：双向 LOCO（4 个 source→target 对） | 完整指标包 ×4 |
| 3.3 | Aim 3：亚洲池 → HRS / SHARE / MHAS | 完整指标包 ×3 |
| 3.4 | SHARE 内部区域异质性 | 亚组表 |
| 3.5 | 漂移诊断全套（SMD / PSI / 测量 / SHAP 一致性） | Figure 5 + 补充 |
| 3.6 | 重校准阶梯 L0→L4 | 重校准表 |
| 3.7 | 8 项敏感性分析 | 补充表 |

## Phase 4：报告与投稿（第 20–24 周）

| # | 任务 | 产出 |
|---|---|---|
| 4.1 | TRIPOD+AI 逐条自查 | 核对表 |
| 4.2 | PROBAST-AI 自评偏倚风险 | 自评表 |
| 4.3 | 图表定稿（5 主图 + 3 主表 + 补充） | 图表包 |
| 4.4 | 正文撰写（可用 `academic-paper` skill 的 `full` 或 `plan` 模式） | 手稿 |
| 4.5 | 模拟审稿（可用 `academic-paper-reviewer` skill） | 审稿意见 + 修改 |
| 4.6 | 期刊选择与 cover letter | 投稿包 |

---

# 第 11 章 工程实现规范

## 11.1 目录结构

```
D:\AI_project\project3\
├── docs\
│   ├── SAP_v1.0.md                  # 冻结的统计分析计划
│   ├── SAP_amendments.md            # 所有偏离记录
│   ├── harmonization_table.xlsx     # 逐变量映射表（核心资产）
│   └── data_dictionary_v1.0.md
├── code\
│   ├── 00_setup\                    # 环境、路径配置
│   ├── 01_extract\                  # 各队列原始 → 长表
│   │   ├── charls_extract.py
│   │   ├── clhls_extract.py
│   │   ├── klosa_extract.py
│   │   ├── hrs_extract.py           # 从 H_HRS_d.dta
│   │   ├── elsa_extract.py          # 从 h_elsa_g3.dta
│   │   ├── share_extract.py         # 从 GH_SHARE_g.dta
│   │   └── mhas_extract.py          # 从 H_MHAS_c2.dta
│   ├── 02_harmonize\                # 统一编码 → analysis-ready
│   │   ├── build_fi.py              # FI 构建（唯一实现，所有队列共用）
│   │   ├── build_ic.py
│   │   └── build_covariates.py
│   ├── 03_outcome\                  # 死亡日期、随访时间、删失
│   ├── 04_develop\                  # CHARLS 开发、nested CV、模型冻结
│   ├── 05_validate\                 # 外部验证、指标计算
│   ├── 06_drift\                    # SMD/PSI/SHAP 一致性
│   ├── 07_recalibrate\
│   ├── 08_figures\
│   └── 09_tables\
├── data\
│   ├── interim\                     # 中间文件（不进 git）
│   └── analysis\                    # analysis-ready 数据集（不进 git）
├── results\
│   ├── literature_extract\          # 已完成（Codex 产出）
│   ├── models\                      # 冻结模型 + 模型卡
│   ├── metrics\                     # 所有性能指标 JSON/CSV
│   ├── figures\
│   └── tables\
├── logs\                            # 每次运行的日志
└── project3_项目计划书_2026-07-26.md
```

**铁律**：`D:\AI_project\sql\` 是只读的原始数据区，任何脚本不得写入该目录。

## 11.2 关键实现约定

**一个 FI 一份代码。** `build_fi.py` 必须是唯一的 FI 实现，接受「队列名 + harmonization 映射表」作为参数。绝不允许为每个队列写一份 FI 代码——那是不一致的最大来源。

**预处理必须封装成可保存的对象。** 用 scikit-learn 的 `Pipeline`：插补器、标准化器、编码器全部装进 pipeline，`joblib.dump` 保存。验证时 `joblib.load` 后直接 `.predict()`。这样在结构上杜绝了「用目标队列信息重新拟合预处理」的泄漏。

```python
# 正确的结构（示意）
from sklearn.pipeline import Pipeline
pipe = Pipeline([("impute", imputer), ("scale", scaler), ("model", clf)])
pipe.fit(X_dev, y_dev)          # 只在开发集 fit
joblib.dump(pipe, "results/models/model_B_frozen.pkl")
# 验证阶段
pipe = joblib.load("results/models/model_B_frozen.pkl")
pred = pipe.predict_proba(X_target)   # 只 transform，绝不 fit
```

**随机种子固定。** `SEED = 20260726`，写进配置文件，所有 `random_state=SEED`。

**每个脚本写日志。** 输入文件、输出文件、样本量、运行时间、包版本。审稿人问「你的 N 怎么从 17708 变成 13421」时，日志是唯一能救你的东西。

**环境锁定。** `requirements.txt` 写死版本号（例：`scikit-learn==1.5.2`）。跨版本 ML 结果会变。

## 11.3 读取各队列的技术要点

| 队列 | 格式 | 读取方式 | 注意 |
|---|---|---|---|
| CHARLS | `.dta` (Stata) | `pandas.read_stata` | 需按 `ID`/`householdID` 跨模块 merge |
| CLHLS | `.sav` (SPSS) | `pyreadstat.read_sav` | 变量名有波次后缀（如 `a1_14`），需仔细核对 codebook |
| KLoSA | `.dta` | `pandas.read_stata` | 主文件 `w01_e.dta`…`w09_e.dta` + exit 文件分开 |
| HRS | `H_HRS_d.dta` | `pandas.read_stata` | 单文件宽表，变量前缀 `r{wave}` |
| ELSA | `h_elsa_g3.dta` | 同上 | + `h_elsa_eol_a2.dta` 补死亡 |
| SHARE | `GH_SHARE_g.dta` | 同上 | 或 `easySHARE_rel9-0-0.dta`（更简单但变量少） |
| MHAS | `H_MHAS_c2.dta` | 同上 | + `H_MHAS_EOL_b.dta` |

**Harmonized 文件的宽表结构**：变量名形如 `r1agey`（wave 1 年龄）、`r5adla`（wave 5 ADL）。前缀 `r` = respondent，`s` = spouse，`h` = household。数字是波次。这是 Gateway 系列的通用命名法，读一次文档就能掌握——`D:\AI_project\sql\Harmonized HRS D 1992-2021.pdf` 是最好的教材，先读它的前 30 页。

**CHARLS 与 KLoSA 需要自己拼装。** 这是 Phase 1 最耗时的部分，预留 3 周。RAND/Gateway 已经替 HRS/ELSA/SHARE/MHAS 做完了这件事。

---

# 第 12 章 24 周时间表

时间估计基于「你没有医学背景、第一次做这类研究、每周投入约 20-25 小时」。如果你能全职投入，可压缩到 14-16 周。

## Phase 1：数据基建（第 1-6 周）

| 周 | 任务 | 交付物 | 完成判据 |
|---|---|---|---|
| 1 | 读 Harmonized HRS 文档前 30 页 + 读 CHARLS 用户手册；跑通每个队列的读取脚本 | 6 个 `*_load_test.py`，各打印维度和前 5 行 | 6 个队列全部能读进 pandas |
| 2 | 队列元数据盘点（表 1 的所有列） | `docs/cohort_inventory.xlsx` | 每队列的基线波次、随访终点、死亡变量名、原始 N 全部填实 |
| 3-4 | Harmonization Table 逐变量填写（这是全项目最重要的 2 周） | `docs/harmonization_table.xlsx` | 45 个 FI 候选项 × 7 队列全部标注可得/不可得 + 原始变量名 |
| 5 | 死亡结局与随访时间构建 + 与已发表死亡率对标 | `03_outcome/` 全部脚本 + 对标表 | 每队列 4 年累积死亡率与文献值差异 < 2 个百分点 |
| 6 | FI/IC 构建 + 分布检查 | `analysis/` 下 7 个 analysis-ready 数据集 | 每队列 FI ≥ 40 项；FI 中位数 0.10-0.35；IC 五域覆盖率报告完成 |

**第 6 周末：Go/No-Go 决策点。** 如果 FI 在某队列无法达到 40 项，或死亡率对标失败，停下来解决，不要进入建模。

## Phase 2：测量等价性 + SAP 冻结（第 7-9 周）

| 周 | 任务 | 交付物 |
|---|---|---|
| 7 | FI 域别构成对比；IC 的 CFA/DIF 检验 | 测量等价性报告 |
| 8 | 决定 IC 定位（主模型变量 or 敏感性模块）；确定 Aim 2/3 最终队列分组 | 决策备忘录 |
| 9 | 撰写并**冻结** SAP v1.0 | `docs/SAP_v1.0.md`（打时间戳，之后只能通过 amendments 修改） |

**SAP 冻结是本项目最重要的仪式性动作。** 冻结之后你才有资格说「预先规定」。可以考虑在 OSF 注册（免费，10 分钟），这会显著提升审稿人对你的信任度。

## Phase 3：开发（第 10-13 周）

| 周 | 任务 | 交付物 |
|---|---|---|
| 10 | Model A-D 的 Cox 基线；nested CV 框架搭建 | 内部性能表（Cox） |
| 11 | 惩罚 Cox + RSF | 内部性能表（+2 模型） |
| 12 | XGBoost + LightGBM；超参调优（在 inner loop 内） | 完整内部性能表 |
| 13 | 模型冻结 + 模型卡撰写 | `results/models/*.pkl` + 8 张模型卡 |

**第 13 周末起，CHARLS 数据不再用于任何模型决策。** 之后如果你修改了模型，必须在论文里如实报告，且所有外部验证要重跑。

## Phase 4：验证（第 14-18 周）

| 周 | 任务 | 交付物 |
|---|---|---|
| 14 | Aim 1：CLHLS 验证 + 漂移诊断 | Aim 1 结果包 |
| 15-16 | Aim 2：双向 LOCO（4 套模型开发 + 验证） | Aim 2 结果包 + 增量收益表 |
| 17 | Aim 3：亚洲池化 → SHARE/HRS/MHAS 三靶验证 | Aim 3 结果包 |
| 18 | 重校准阶梯 + 亚组分析 + 全部敏感性分析 | 补充材料结果包 |

## Phase 5：产出（第 19-24 周）

| 周 | 任务 | 交付物 |
|---|---|---|
| 19-20 | 5 张主图 + 4 张主表定稿 | `results/figures/`、`results/tables/` |
| 21-22 | 论文初稿（Methods 先写，因为最难且最长） | 全文初稿 |
| 23 | TRIPOD+AI 清单逐条核对；模拟审稿（可用 `/ars-reviewer`） | 核对表 + 修订稿 |
| 24 | 投稿准备：cover letter、数据可得性声明、伦理声明 | 投稿包 |

## 12.1 里程碑与止损点

| 里程碑 | 时间 | 若未达成的处理 |
|---|---|---|
| M1：7 队列全部读通 | 第 1 周末 | 优先解决，这是硬前提 |
| M2：Harmonization Table 完成 | 第 4 周末 | 可延 1 周，但不能跳过 |
| M3：死亡率对标通过 | 第 5 周末 | **止损点**：对标不通过说明结局构建错了，继续下去全部白做 |
| M4：FI ≥ 40 项 × 7 队列 | 第 6 周末 | 若某队列只能到 35 项，降级该队列为敏感性分析对象 |
| M5：SAP 冻结 | 第 9 周末 | 硬性 |
| M6：模型冻结 | 第 13 周末 | 硬性 |
| M7：Aim 1 完成 | 第 14 周末 | 若 CLHLS 外部 C 指数 < 0.60，暂停并排查（正常应在 0.68-0.78） |
| M8：全部验证完成 | 第 18 周末 | — |
| M9：初稿完成 | 第 22 周末 | — |

---

# 第 13 章 图表清单（主文 5 图 4 表 + 补充）

## 13.1 主文图

**Figure 1：研究设计与队列时间线。**
上半部分：6 条横向时间轴，每条一个队列，标出所有波次的年份、选定的基线波次（加粗）、4 年结局窗口（阴影）、7 年窗口（虚线阴影）。下半部分：三个 Aim 的 source→target 箭头图。
读者从这一张图应能立刻回答：谁开发、谁验证、时间窗多长。

**Figure 2：样本流程与变量可得性。**
左：6 个并列的 CONSORT 式流程图（原始 N → 各排除步骤 → 分析 N → 事件数）。
右：热图，行为 45 个 FI 候选项 + IC 五域，列为 6 队列，颜色表示可得/代理/不可得。

**Figure 3：source-target 性能矩阵。**
主体是一张热图：行为 source（CHARLS、亚洲池化、各 LOCO 组合），列为 target（6 队列），单元格数字为 4 年 time-dependent AUC（含 95% CI）。对角线（内部验证）用不同色系区分，避免读者误读为外部结果。
下方三个分面板：C 指数、Brier score、校准斜率的同构热图。

**Figure 4：校准与临床效用。**
2 行 × 4 列。第一行：4 个主要 target 队列的校准曲线（含 flexible calibration curve + 十分位点 + 45° 参考线）。第二行：对应的 DCA（横轴阈值 0-40%，纵轴净获益，三条线：模型/全部干预/不干预）。

**Figure 5：解释稳定性。**
Panel A：CHARLS 开发集的 SHAP beeswarm（top 20 特征）。
Panel B：跨 7 队列的 SHAP 重要性排名热图 + Spearman 一致性系数矩阵。
Panel C：FI 与 IC 五域的 SHAP dependence 曲线，7 队列叠加，看形状是否一致。

## 13.2 主文表

**Table 1：基线特征。** 行为变量，列为 6 队列，含 SMD 列（相对 CHARLS）。这张表会很大，可能需要横排或拆为 1a/1b。

**Table 2：预测因子模块定义。** FI 45 项清单 × 队列可得性 × 编码规则；IC 五域的操作化定义。（如太长则移入补充材料，主文放精简版）

**Table 3：模型性能汇总。** 行为 model × source-target 对，列为 C 指数、5y AUC、Brier、IBS、校准截距、校准斜率、DCA 净获益（阈值 10%），全部含 95% CI。

**Table 4：迁移诊断。** 行为 target 队列，列为事件率、总体 PSI、SMD > 0.10 的变量数、FI 项数、SHAP 排名一致性、校准阶梯四层的 C 指数/校准斜率。这张表是回答「为什么性能会变」的核心证据。

## 13.3 补充材料

| 编号 | 内容 |
|---|---|
| S1 | 完整 Harmonization Table（逐变量、逐队列、原始变量名、编码规则） |
| S2 | 缺失率热图 + 缺失机制探查 |
| S3 | IC 的 CFA 拟合指标 + IRT-DIF 结果 |
| S4 | 完整超参数搜索空间与最优值 |
| S5 | 8 年死亡敏感性分析全套结果 |
| S6 | 失能次要结局全套结果 |
| S7 | 亚组校准（年龄 × 性别 × 教育 × 城乡 × 国家） |
| S8 | SHARE 国家级 leave-one-country-out 结果 |
| S9 | 加权 vs 未加权对比 |
| S10 | FI 核心 30 项 vs 全 45 项对比 |
| S11 | 8 张模型卡 |
| S12 | TRIPOD+AI 27 条核对表（含每条在正文的位置） |
| S13 | PROBAST-AI 自评 |
| S14 | 全部代码（GitHub 链接 + commit hash） |

---

# 第 14 章 报告规范与可复现性

## 14.1 必须遵守的三份指南

**TRIPOD+AI（2024）** — 预测模型研究的报告标准，27 个条目。这是审稿人会拿来逐条对照的清单。投稿前必须填完，并把填好的表放进补充材料。核心条目里最容易被扣分的是：

- 条目 7（预测因子）：必须说明预测因子如何测量、何时测量、是否对结局盲法。
- 条目 12（模型开发）：必须说明超参数如何选、变量如何选、有没有用到验证集信息。
- 条目 15（模型性能）：必须同时报告判别和校准。只报 AUC 会被直接指出。
- 条目 20（局限性）：必须讨论测量异质性和适用范围。

**PROBAST-AI** — 偏倚风险评估工具，4 个领域（参与者、预测因子、结局、分析）。这是给审稿人用的，但你应该先自己评一遍，把每个「高风险」项在讨论部分主动交代。自评比被指出好。

**RECORD / STROBE** — 使用既有数据的观察性研究报告规范，用于补充 TRIPOD 未覆盖的数据来源描述部分。

## 14.2 代码与工程规范

目录结构建议：

```
D:/AI_project/project3/
├── code/
│   ├── 00_setup/          环境、路径、随机种子
│   ├── 01_extract/        逐队列原始数据读取
│   ├── 02_harmonize/      变量映射，产出 harmonization table
│   ├── 03_construct/      FI、IC、协变量、结局
│   ├── 04_cohort/         纳排、样本流程、Table 1
│   ├── 05_model/          训练、冻结、序列化
│   ├── 06_validate/       各 source-target 对的评价
│   ├── 07_diagnose/       漂移、SHAP、校准阶梯
│   ├── 08_figures/        出图
│   └── 99_report/         汇总表格、TRIPOD 核对
├── data_derived/          派生分析数据集（不放原始数据）
├── models/                冻结的模型文件 + 模型卡
├── results/
│   ├── literature_extract/   （已有）
│   ├── tables/
│   ├── figures/
│   └── logs/
└── docs/
    ├── SAP.md             统计分析计划（冻结版）
    ├── harmonization_table.xlsx
    └── decision_log.md    所有偏离 SAP 的决定及理由
```

铁律：

1. **原始数据只读。** `D:/AI_project/sql/` 下的文件永不写入。所有派生数据写到 `data_derived/`。
2. **随机种子全局固定。** 在 `00_setup` 里设一个 `SEED = 20260726`，所有涉及随机的步骤（划分、插补、bootstrap、模型初始化）都从它派生。记录在模型卡里。
3. **decision_log.md。** 任何偏离 SAP 的决定都要记：日期、原来计划什么、改成什么、为什么、谁决定的。审稿人问「你是不是试了很多种做法才选了这个」时，这份日志是你唯一的辩护。
4. **模型冻结用序列化文件。** 不要「重跑一遍代码得到同样的模型」，要把训练好的模型对象存成文件（pickle / joblib / ONNX），验证脚本只加载不训练。这样物理上不可能发生信息泄漏。
5. **环境锁定。** `requirements.txt` 写精确版本号。scikit-learn 不同小版本的默认参数会变。

## 14.3 数据安全

所有数据均为去标识化公开研究数据，但仍需遵守各数据提供方的使用协议：

- 不要把任何个体级数据上传到 GitHub、云盘或 AI 服务。代码可以公开，数据不行。
- 论文补充材料只放汇总统计和模型系数，不放个体记录。
- SHARE、HRS、KLoSA 的使用协议通常要求在致谢中写明特定的资助声明文本。投稿前逐个数据源查一遍官网的 acknowledgment requirement，抄准确。
- 如需伦理审查豁免：多数机构对公开去标识化二手数据给豁免，但要走流程拿到豁免函编号，论文伦理声明里要写。这件事现在就去问你单位的伦理委员会，别等到投稿前。

---

# 第 15 章 目标期刊与投稿策略

## 15.1 期刊分级

| 等级 | 期刊 | IF（约） | 定位 | 适合本研究的理由 |
|---|---|---|---|---|
| 首选 | *Lancet Healthy Longevity* | ~50 | 老年健康旗舰 | 明确鼓励跨文化老龄化研究，对 ML 方法开放，2023 年有 2 篇结构相近的发表 |
| 首选 | *Nature Aging* | ~17 | 衰老机制+人群 | 方法学创新文章接受度高，但通常需要更深的生物学机制解读 |
| 备选 | *Age and Ageing* | ~8 | 老年医学综合 | 最合适本研究的实用主义定位；审稿周期短；IF 增长明显 |
| 备选 | *J Gerontology: Series A* | ~6 | 老年生物/医学 | HRS 系列研究的传统发表平台，编辑委员会熟悉多队列设计 |
| 保底 | *BMC Geriatrics* | ~4 | 开放获取 | 接受率较高，预测模型方法文章有固定受众 |
| 方法学首选 | *BMJ Open* / *PLOS Medicine* | ~3-5 | 方法学透明 | 若论文主要卖点是方法而非临床结论 |

## 15.2 实际建议

给没有发表过第一篇 SCI 的研究者的建议：

**不要一开始就冲 Lancet。** 首篇论文最大的风险是被拒后失去信心，或者审稿意见你无法回应。建议首选 *Age and Ageing* 或 *J Gerontology*——两者都有 6 个队列跨文化设计的发表先例，审稿人熟悉这类研究，意见可以应对。

**同时做一份「快审版」**：把复杂的方法学细节移入补充材料，正文控制在 3500 字以内，配 1 张精华图（Figure 3 的热图），适合 *Age and Ageing* 的 3500 字限制。这不是降级，而是期刊适配。

**预印本发布**：在正式投稿前，把手稿传到 medRxiv。理由：（a）锁定优先权，（b）建立预先注册时间戳，（c）可以通过 Twitter/X 获得同行非正式反馈，提升投稿质量。CHARLS/HRS 系的研究者圈子很活跃。

---

# 第 16 章 模拟审稿意见与应对（SCI 投稿前自测）

这一章是我以资深流行病学审稿人的角度，对你的方案提前提出的批评。每一条都有应对方案。**在写完论文后用这一章做自测，能避免 80% 的修回意见。**

## 16.1 方法学批评（最难的一类）

**批评 1（最高频）**："你的研究声称是外部验证，但如何确保开发集没有使用任何目标队列信息（no contamination）？"

> **应对**：（a）说明模型冻结时间戳早于任何目标队列数据的载入时间；（b）提供 git commit log 作为证据；（c）说明 CHARLS 插补模型只用 CHARLS 内部变量拟合；（d）目标队列预处理独立运行，使用开发集的标准化参数（写进 Methods）。

**批评 2（高频）**："FI 在不同队列中构建的 deficit 项目数不同（CHARLS 42 项 vs KLoSA 38 项），你怎么能说它们是同一个预测因子？"

> **应对**：（a）FI 用比例计算（有问题项数 / 被问到项数），与项目总数无关；（b）提供 FI_core（共同核心项）的敏感性分析，结果与 FI_full 一致；（c）引用 Searle 2008 的定义和已发表的方法学依据。

**批评 3（高频）**："你使用 MICE 填补缺失值，但这会把测量误差/样本选择偏倚掩盖掉，特别是在目标队列中。"

> **应对**：（a）报告每个队列、每个变量的缺失率（补充表）；（b）提供完整病例分析的敏感性分析；（c）说明目标队列插补时没有使用结局变量；（d）插补模型包含所有将进入正式分析的变量（减少 MAR 假设依赖）。

**批评 4（中频）**："你的 Aim 2 结论是「训练数据多样性改善迁移」，但这也可能只是样本量增加的效果。"

> **应对**：这正是降采样对照设计的目的（见 SAP 9.1 节）。报告降采样 200 次的 C-index 分布，若差异在降采样后收敛到零，则诚实说「我们无法区分多样性效应与样本量效应」——这是诚实的科学，比强行宣称「多样性有用」更可发表。

**批评 5（中频）**："你对「全球推广」的声称过于宽泛，你没有任何非洲、东南亚、南亚数据。"

> **应对**：（a）修改摘要结论，只说「在欧洲/美国/英国/墨西哥队列的外部验证」；（b）在 Discussion Limitations 有专门一段讨论覆盖范围的边界；（c）把 MHAS 的存在作为「中高收入国家」的代表，同时承认其不能代表低收入国家。**这条批评可以预防，方法是从一开始就不夸大。**

## 16.2 数据来源类批评

**批评 6**："CHARLS 的自报共病是否经过医学确认？"

> **应对**：（a）说明研究是预测研究，自报疾病作为预测因子，其预测价值不依赖于诊断的准确性（但会在 Limitation 中指出「自报诊断可能存在测量误差」）；（b）加引 CHARLS 已发表的效度研究（如与体检数据的对比）；（c）提供不含自报共病、只用体检客观指标的敏感性分析——但这会大幅降低覆盖率，所以要预先规定此为敏感性而非主分析。

**批评 7**："CLHLS 的死亡日期质量如何？百岁老人的死亡证明是否可靠？"

> **应对**：（a）提供死亡率对标数据（与中国官方死亡统计比较）；（b）说明死亡信息来源（家属报告 + 户籍记录双核实，CLHLS 的标准程序）；（c）做去掉 90+ 岁者的敏感性分析，如果结论不变则增强稳健性论证。

## 16.3 解释类批评

**批评 8**："你说衰弱是「强预测因子」，这是否意味着衰弱干预能降低死亡风险？"

> **应对**：这是因果 vs 预测的混淆。明确写：「本研究结果反映的是预测关联，不是因果效应，因此不能直接推断衰弱干预对死亡的效果」。在 Discussion 第一段就写清楚研究定位，之后就算审稿人问，也能指回这一段。

---

# 第 17 章 风险登记表

提前识别风险、备好应对，防止某一步骤卡死整个项目。

| # | 风险 | 概率 | 影响 | 应对方案 |
|---|---|---|---|---|
| R1 | CHARLS 死亡信息在 2015/2018 不完整 | 高 | 高 | Phase 1 第 5 周必须核查；备选：把基线改为 2013，验证期延到 2018/2020 |
| R2 | KLoSA 事件数不足（< 100） | 中 | 高 | 若 w03/w04 EXIT 前缺失，可合并多波 EXIT；若仍不足，降级为 Aim 2 的补充敏感性 |
| R3 | Gateway Harmonized CHARLS 下载受阻或版本不匹配 | 中 | 高 | 备选：参照 `Harmonized_CHARLS_D.pdf` 自建，Phase 1 延长 2 周 |
| R4 | IC 五域跨队列 scalar invariance 不成立 | 高（预期） | 中 | 这是预期结果，不是失败。按方案把 IC 降级为并行模块，并用 IC 结果写一篇独立的第二篇论文 |
| R5 | SHARE 某些国家缺失率过高（>40% 某域） | 中 | 低 | 在国家级 LOCO 分析中排除该国，在方法中说明 |
| R6 | 外部验证 C-index < 0.60 | 低 | 高 | 分解原因；若是死亡率对标失败则检查结局构建；若是真实可迁移性极差，这也是可发表的负面结论 |
| R7 | 代码跨系统/版本运行不一致 | 中 | 中 | 用 `requirements.txt` 锁版本；关键步骤输出哈希值 |
| R8 | 计算资源不足（数据量大、bootstrap 慢） | 低 | 低 | SHARE 主文件 6.2 GB 但宽表读一次即可；bootstrap 1000 次最多几小时，可分批跑 |
| R9 | 伦理委员会豁免申请延误 | 中 | 中 | 现在就去提交，写完论文才申请是最常见的延迟来源 |
| R10 | 审稿人要求加入 JSTAR | 低 | 中 | 在 Limitation 中主动写「本研究未涵盖日本数据」并给出理由（未能获取）；预先准备这个回复 |

---

# 第 18 章 推荐学习路径（针对无医学背景的你）

按优先级排序。前三本是必读，其余按需要的时候读。

## 18.1 必读（每本控制在 30-50 页精读，其余略读）

| 文献 | 内容 | 在哪找 |
|---|---|---|
| Moons KGM et al. (2015) TRIPOD statement, *Ann Intern Med* | 预测模型报告规范，奠定你对「好的预测研究长什么样」的基本认知 | 期刊官网免费 |
| Steyerberg EW *Clinical Prediction Models* (2019, 2nd ed) | 预测模型的统计教材，先读第 2、4、15、22 章 | Springer，学校图书馆通常有电子版 |
| Riley RD et al. (2021) Minimum sample size for external validation | 外部验证的样本量问题，本项目的样本量论证依据 | *BMJ* 免费 |

## 18.2 本项目直接相关的核心文献（已在 literature_extract 里，但要亲自读）

| 标签 | 内容 |
|---|---|
| C02 He et al. | CHARLS → CLHLS-HF，你的 Aim 1 最直接的蓝本 |
| C03 Yang & Zhang | SHARE → HRS → CHARLS，跨三队列 ML |
| C07 Suemoto et al. | 五队列十年死亡传统模型，为你提供 Cox 基线和校准报告模板 |
| C08 Stolz et al. | 四队列 FI 变化与死亡，FI 文献的基础证据 |
| C16 Olender et al. | ML 老年预测的 meta-analysis，你的方法论批判基础 |
| C25 Lee et al. | Gateway to Global Aging Data，了解 harmonized 文件的哲学 |

## 18.3 衰弱指数方法学（建议读原始文献，不要只看综述）

- Searle SD et al. (2008). A standard procedure for creating a frailty index. *Age and Ageing*.（FI 构建的「宪法」，全文精读）
- Rockwood K et al. (2005). A global clinical measure of fitness and frailty. *CMAJ*.（FI 的奠基论文）

## 18.4 校准（不要跳过，这是你论文被接受的关键）

- Van Calster B et al. (2019). Calibration: the Achilles heel of predictive analytics. *BMC Medicine*.（免费，解释为什么 AUC 不够）
- Austin PC et al. (2019). Graphical calibration curves and the integrated calibration index. *Statistics in Medicine*.

## 18.5 工具库（Python/R）

Python 生态：

```python
# 生存分析
from lifelines import CoxPHFitter, KaplanMeierFitter
from sksurv.ensemble import RandomSurvivalForest
from sksurv.linear_model import CoxnetSurvivalAnalysis

# 校准
from sklearn.calibration import calibration_curve
# 外加手写 logistic recalibration（2 行代码）

# SHAP
import shap

# 插补
from sklearn.experimental import enable_iterative_imputer
from sklearn.impute import IterativeImputer  # MICE 的简化版
```

R 生态（校准和 DCA 建议用 R）：

```r
library(riskRegression)  # C-index、Brier、AUC（生存版），作者 Mogensen/Gerds
library(pec)             # 另一套 Brier/校准
library(dcurves)         # Decision Curve Analysis
library(CalibrationCurves) # 灵活校准曲线
library(survival)        # Cox 模型
library(mice)            # 最成熟的 MICE 实现
```

---

# 第 19 章 附录：本项目设计的常见问题 Q&A

**Q1：我需要有医学执照或医学学位才能做这个研究吗？**

不需要。这是流行病学方法学研究，使用去标识化的公开数据，你的分析工具是统计与机器学习，不是临床诊断。多数发表这类研究的通讯作者来自统计、公共卫生、计算机科学背景。你需要的是：统计方法上的诚实、对数据的细心，以及理解你的研究边界（不能告诉别人「用这个模型帮患者做决定」）。

**Q2：是否需要伦理审批？**

所有队列数据已在获取时经过去标识化处理，且受访者已在原始研究时签署知情同意。多数机构对二手去标识化数据给予伦理豁免（exemption），但需要走程序拿到豁免函编号，投稿时需要写进方法部分。去你单位的 IRB/伦理委员会问，不要等到投稿前。

**Q3：需不需要 Stata？我只会 Python。**

主要分析用 Python 完全可行（`scikit-survival`、`lifelines`）。但校准曲线和 DCA 的 R 生态更成熟（`riskRegression`、`dcurves`），审稿人更熟悉这些包的输出格式。建议至少用 R 做这两件事，其余用 Python。Python ↔ R 可以用 `rpy2` 或者直接互写中间文件。

**Q4：这个研究能发顶刊吗？**

诚实评估：作为第一篇论文，冲 Lancet / Nature Aging 的概率不高（需要多年声誉积累和顶级合作团队），但投 *Age and Ageing* 或 *J Gerontology* 是非常现实的目标，Q1 期刊，IF 在 6-8 之间，在老年医学领域认可度很高。如果方法学执行到位（特别是降采样对照和校准阶梯），论文质量有充分支撑，投 Lancet Healthy Longevity 作为一次尝试也不失为合理选择——最坏结果是审稿意见帮你改得更好。

**Q5：Codex 能负责哪些部分？**

参考 `CLAUDE.md` 的角色分工：

- Claude（我）：WHY（为什么做）、WHAT（做什么）→ 研究设计、方法论决策、论文思路
- Codex：HOW（如何实现）→ 数据提取脚本、FI 构建代码、模型训练、评价指标计算

你的决策节点（第 0.3 节、Gate 1、Gate 2）是不能外包的。数据盘点（第 6 章的 7 项任务）需要你本人核查，因为 Codex 看到的数字要人来确认它的医学合理性。

---

# 附录 A：关键缩写

| 缩写 | 全称 |
|---|---|
| FI | Frailty Index，衰弱指数 |
| IC | Intrinsic Capacity，内在能力 |
| LOCO | Leave-One-Cohort-Out，留一队列法 |
| SAP | Statistical Analysis Plan，统计分析计划 |
| MICE | Multiple Imputation by Chained Equations |
| IPCW | Inverse Probability of Censoring Weighting |
| DCA | Decision Curve Analysis，决策曲线分析 |
| SHAP | SHapley Additive exPlanations |
| SMD | Standardized Mean Difference，标准化均差 |
| PSI | Population Stability Index |
| IBS | Integrated Brier Score |
| IPA | Index of Prediction Accuracy |
| AUC | Area Under the ROC Curve |
| C-index | Concordance index，一致性指数 |
| O:E | Observed-to-Expected ratio，观察/预期比 |
| TRIPOD | Transparent Reporting of a multivariable prediction model |
| PROBAST | Prediction model Risk Of Bias Assessment Tool |
| DIF | Differential Item Functioning |
| CFA | Confirmatory Factor Analysis，验证性因素分析 |
| IRT | Item Response Theory，项目反应理论 |
| HARKing | Hypothesizing After Results are Known |
| EPV | Events Per Variable |
| EOL | End of Life（生命终期模块） |
| CHARLS | China Health and Retirement Longitudinal Study |
| CLHLS | Chinese Longitudinal Healthy Longevity Survey |
| HRS | Health and Retirement Study（美国） |
| ELSA | English Longitudinal Study of Ageing |
| SHARE | Survey of Health, Ageing and Retirement in Europe |
| MHAS | Mexican Health and Aging Study |
| KLoSA | Korean Longitudinal Study of Ageing |

---

*文档结束。Version 1.0 — 2026-07-26*
*基于 26 篇核心文献 + D:/AI_project/sql/ 磁盘实际核查撰写。*
*任何对 SAP 的修改请记录在 docs/SAP_amendments.md 中。*

> **v2.1 更新**：2026-07-29 — 事件数修正（D-021/D-022/D-023/D-024 final），ELSA 移除（D-013），7→6 队列统一，5年→4年结局统一，EPV 表修正。六队列就绪注册表见 `results/cohort_readiness/six_cohort_readiness_registry_2026-07-29.md`。
